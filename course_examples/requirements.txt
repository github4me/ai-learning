# Historical course pin; this revision has not rerun the PyTorch examples.
# The current editing environment has Python 3.12 but no installed PyTorch.
# For a GPU-specific build, follow the official PyTorch installation selector.
torch==2.14.0

