{
  "本周重点不是实现四套 tokenizer，而是让数据可靠地进入已经学过的模型。主线沿用五词表与 course_data.py；字符和 BPE 是解释替代方案的独立实验。先学会一个可靠入口，再比较更复杂方案。": "This week focuses on a reliable data interface, not implementing four production tokenizers. The main model path keeps the five-token vocabulary and course_data.py. Character and BPE examples are separate demonstrations of alternatives. Learn one reliable input path before comparing more complex schemes.",
  "学习单元": "Study unit",
  "本次解决的问题": "The question we solve",
  "一：稳定的文字接口": "1: A stable text interface",
  "固定分词规则、ID 顺序和未知词行为；重复编码不能重新随机编号。": "Fix segmentation, ID order, and unknown-token behavior. Encoding again must not randomly renumber the vocabulary.",
  "二：真的构造数据": "2: Construct actual data",
  "同一数据函数从文档构造 T+1 窗口；先分训练/验证，再各自切窗。": "Build T+1-token windows from documents using shared data functions. Split training and validation documents first, then construct windows within each split.",
  "三：判断实验是否可信": "3: Decide what an experiment demonstrates",
  "检查文档重复、长片段重叠和有效目标数，分清流程演示与独立验证。": "Check duplicate documents, long shared passages, and valid-target counts. Distinguish a pipeline demonstration from independent validation.",
  "四：按需要比较方案": "4: Compare alternatives as needed",
  "先可读字符 BPE 的两轮合并，再选读字节、特殊 token 和 padding；不要求这些成为五词模型的前置。": "Start with two readable BPE merges, then optionally examine bytes, special tokens, and padding. These are not hidden prerequisites for the five-token model.",
  "运行 python week09_data_protocol.py；它直接产生 Week 10 能接收的五词 inputs/targets。最终项目会明确新建字符词表，不会把另一套 ID 悄悄送进已有 checkpoint。": "Run python week09_data_protocol.py from the examples directory. It produces the five-token inputs/targets accepted by Week 10. The final project explicitly creates a separate character vocabulary instead of silently feeding a different ID mapping into an existing checkpoint.",
  "建议阅读、手算、改代码交替进行，每个单元可拆成几次完成。章节编号保留用于旧链接和回查；按页面从上到下的新顺序学习，不需要按旧编号来回跳转。": "Alternate reading, hand calculation, and code changes. Units can take several sessions. Existing section numbers remain for links and reference; follow the displayed order rather than jumping around by older numbers.",
  "Week 9 核心目标：让文字与模型共享一份稳定协议": "Week 9 learning goal: a stable shared protocol between text and model",
  "先看问题": "Start with the problem",
  "用户输入开放的 Unicode 文字，神经网络却只接受有限范围的整数地址。若切分规则或 ID 对照表改变，同一个数字就可能指向另一段文字，已训练的 embedding row 也随之被误用。": "Users enter Unicode text, but an embedding layer accepts integer indices from a finite range. If segmentation or the ID mapping changes, the same number may refer to different text and retrieve a row learned for another token.",
  "为什么需要它": "Why we need this",
  "把 Tokenizer 定义为 raw text 与模型之间可保存、可验证、可版本化的协议。": "Define the tokenizer as a saved, checkable, versioned protocol between raw text and the model.",
  "用同一句“我喜欢AI，AI也喜欢猫。”走完单位选择、BPE、特殊 token、stream、shift 与 batch。": "Use the Chinese example “我喜欢AI，AI也喜欢猫。” (“I like AI, and AI also likes cats.”) to trace segmentation, BPE, special tokens, streams, shifting, and batches. The Chinese text is intentional data for Unicode examples, not untranslated explanation. Parenthesized English meanings are not part of its token strings.",
  "先把本周压缩成三件事": "First reduce the chapter to three tasks",
  "① 同一段文字按固定规则变成一串编号；② 训练与生成始终使用同一份编号表；③ 连续编号右移一位，构成输入和答案。先掌握这三件事，再回看 UTF-8、BPE 每轮合并和特殊 token 的细节。": "① Turn text into IDs using fixed rules. ② Use the same ID mapping for training and generation. ③ Shift a contiguous token sequence by one position to form inputs and targets. Learn these first, then revisit UTF-8, BPE merges, and special-token details.",
  "本章有两套明确分开的演示：字节 BPE 说明规则怎样学出来；w09-readable-v1 用可读编号说明训练数据怎样排列。你不需要把十六进制转换练习当成进入 Week 10 的考试。": "The detailed worked tracks are byte BPE, showing how merge rules are learned, and w09-readable-v1, showing data layout with readable IDs. There is also a short character-tokenizer warm-up and a bridge back to the five-token model. Hexadecimal conversion is not an entrance exam for Week 10.",
  "Tokenizer 不是语言模型。它规定 normalization、怎样切分、Vocabulary 中有哪些 token、每个 token 对应哪个 ID，以及 IDs 怎样 decode。它在模型前把文字变成整数，也在生成后把整数还原为文字；真正可学习的 contextual patterns 位于 embedding 与 Transformer 参数中。": "A tokenizer is not the language model. It specifies normalization, segmentation, vocabulary entries, token IDs, and decoding. It converts text into integers before model computation and generated integers into text afterward. The contextual patterns learned by the language model reside in its embedding and Transformer parameters, not in the act of encoding.",
  "先抓住 Token 的实际意义": "What a token means operationally",
  "一个 token 会占用一个 context position、触发一次 embedding lookup，并在 next-token training 中对应一次预测任务。Tokenizer 怎样切分，不只改变显示方式，也会改变 sequence length、可见上下文范围和训练计算量。": "A token occupies a context position and selects an embedding row. In shifted training data, input positions are paired with next-token prediction tasks. Segmentation changes sequence length, how much text fits in context, and computational cost—not just display formatting.",
  "概念": "Concept",
  "本章含义": "Meaning in this chapter",
  "固定句中的例子": "Example from the fixed sentence",
  "Tokenizer 发出的一个离散计算单位": "A discrete unit emitted by a tokenizer",
  "可以是 我、喜欢、AI，也可能是某个 byte": "It might be 我, 喜欢, AI, or an individual byte",
  "允许 token 与 ID 的有限清单": "A finite list of allowed tokens and their IDs",
  "w09-readable-v1 有 V=11 个 entries": "w09-readable-v1 contains V=11 entries",
  "某个 entry 的整数地址": "An integer index for an entry",
  "在该快照中 AI 的地址是 6": "In this snapshot, AI has ID 6",
  "normalization、split、encode、decode 与 special-token policy 的整体协议": "The complete normalization, segmentation, encoding, decoding, and special-token policy",
  "将固定句映射成下方 stream": "Map the fixed sentence into the stream below",
  "w09-readable-v1 的完整 ordered Vocabulary（V=11）": "The complete ordered vocabulary of w09-readable-v1 (V=11)",
  "文档开始": "Document start",
  "文档结束": "Document end",
  "补齐 batch 长度": "Pad batch sequences to a common length",
  "一种有损的未知项策略": "A lossy unknown-item policy",
  "我": "我 (I)",
  "内容 token": "Content token",
  "喜欢": "喜欢 (like)",
  "也": "也 (also)",
  "猫": "猫 (cat)",
  "我喜欢AI，AI也喜欢猫。": "我喜欢AI，AI也喜欢猫。",
  "[<BOS>, 我, 喜欢, AI, ，, AI, 也, 喜欢, 猫, 。, <EOS>]": "[<BOS>, 我, 喜欢, AI, ，, AI, 也, 喜欢, 猫, 。, <EOS>]",
  "decode(skip_special_tokens=True) → 我喜欢AI，AI也喜欢猫。": "decode(skip_special_tokens=True) → 我喜欢AI，AI也喜欢猫。",
  "同一个数字只能在自己的 artifact 内解释；本章不会把这几套 IDs 混在一起": "Interpret IDs only within their own tokenizer artifact; do not mix these mappings",
  "教学 artifact": "Teaching artifact",
  "使用位置": "Where used",
  "基本单位 / ID space": "Base units / ID space",
  "学习目的": "Learning purpose",
  "何时结束": "Where this example ends",
  "第 4 节": "Section 4",
  "9 个已知 code points，IDs 0..8": "Nine known Unicode code points, IDs 0..8",
  "看清 encode、decode、dtype 与 [L]": "Understand encode, decode, dtype, and [L]",
  "第 4 节结束": "End of Section 4",
  "第 5–7 节": "Sections 5–7",
  "UTF-8 bytes 0..255，再建立 256、257": "UTF-8 byte IDs 0..255, then merged IDs 256 and 257",
  "手算 pair count、merge、冻结与还原": "Calculate pair counts, merges, frozen encoding, and reconstruction",
  "第 7 节结束": "End of Section 7",
  "第 8–18 节": "Sections 8–18",
  "可读 pieces，V=11，IDs 0..10": "Readable pieces, V=11, IDs 0..10",
  "走完 special stream、window、mask 与 batch": "Trace the special-token stream, windows, masks, and batches",
  "Week 9 结束": "End of Week 9",
  "Week 10 起": "From Week 10",
  "五个 tokens，V=5，IDs 0..4": "Five tokens, V=5, IDs 0..4",
  "连接 canonical MiniGPT": "Connect to the canonical MiniGPT",
  "按 Week 10–12 协议": "According to the Weeks 10–12 protocol",
  "在固定 tokenizer artifact A 下，encode 产生长度 L 的合法整数序列，decode 按同一 artifact 的规则重建文字。": "With a fixed tokenizer artifact A, encode produces a valid integer sequence of length L, and decode reconstructs text according to that same artifact's rules.",
  "本章的可读快照": "The chapter's readable snapshot",
  "后半章用教学 artifact w09-readable-v1：V=11、special IDs 0..3、完整 stream 长度 L=11、batch context T=4。它只服务 Week 9，绝不是通用或 production tokenizer。": "The later sections use w09-readable-v1: V=11, special IDs 0..3, full stream length L=11, and context T=4. It is a Week 9 teaching artifact, not a general-purpose production tokenizer.",
  "阅读时先问自己：现在在哪个 ID space？": "Keep asking: which ID space am I in?",
  "看到 256、257 时是在 toy-byte-bpe；看到 AI=6、猫=9 时是在 w09-readable-v1；看到 Week 10 的 AI=2 时已经切换到 mini-gpt-v1。数值相同或不同都不表达语义关系，它们只是各自表内的地址。": "IDs 256 and 257 refer to toy-byte-bpe. AI=6 and 猫=9 belong to w09-readable-v1. Week 10's AI=2 belongs to mini-gpt-v1. Matching or differing numbers do not imply semantic relationships; they are addresses in separate tables.",
  "常见误区": "Common misconceptions",
  "Token ID 是地址，不带“6 比 5 更像 AI”的数值语义。": "A token ID is an address; 6 is not inherently “more like AI” than 5.",
  "Tokenizer 的 decode 能否 exact round-trip 取决于 normalization、fallback 与 special-token policy，不能无条件保证。": "Exact encode/decode round-tripping depends on normalization, fallback, and special-token policies. It is not an unconditional guarantee.",
  "只检查两个 tokenizer 的 V 相等，不能证明它们与同一 checkpoint 兼容。": "Equal vocabulary sizes do not prove that two tokenizers are compatible with the same checkpoint.",
  "为什么 tokenizer artifacts 必须与 model checkpoint 一起版本化？": "Why version tokenizer artifacts together with model checkpoints?",
  "embedding row 与 LM-head column 都按 ID 学习。只有 normalization、切分、ordered vocabulary、merges 与 special IDs 完全一致，某个地址才仍代表训练时的同一 token。": "Embedding and output-head entries are learned according to token IDs. Normalization, segmentation, ordered vocabulary, merges, and special IDs must match the training protocol so that each index still represents the intended token.",
  "1. Token 不等于 Word：同一句话可以有四种边界": "1. A token is not necessarily a word: four possible boundaries",
  "把 token 默认理解成“单词”会在中文、标点、代码、emoji 和 byte fallback 上立刻失效，也会让人误以为 token 数等于字符数。": "Equating tokens with words fails for Chinese text, punctuation, code, emoji, and byte fallback. Token counts are not generally character counts either.",
  "比较四种明确的 segmentation rule，而不是寻找唯一“语言学正确”的切法。": "Compare four explicit segmentation rules rather than searching for one uniquely correct linguistic segmentation.",
  "说明 token count L 是 tokenizer 的计算结果，并直接影响 context window 能容纳多少文字。": "Explain that token count L is an output of the tokenizer and affects how much text fits in a context window.",
  "对“我喜欢AI，AI也喜欢猫。”，同一 raw text 在不同规则下可以发出不同数量的单位。下面的 word-like row 依赖指定 segmenter；subword row 恰好看起来一样，只是这个教学词表训练后的结果，并非 subword 的定义。": "The same text, “我喜欢AI，AI也喜欢猫。”, can produce different units under different rules. The word-like row below depends on a specified segmenter. The illustrative subword row happens to look the same, but that is a result of its chosen vocabulary, not the definition of subword tokenization.",
  "同一固定句的四种明确切分": "Four explicit segmentations of the fixed sentence",
  "[我, 喜, 欢, A, I, ，, A, I, 也, 喜, 欢, 猫, 。]": "[我, 喜, 欢, A, I, ，, A, I, 也, 喜, 欢, 猫, 。]",
  "这些 code points 无需 word dictionary": "Code-point boundaries do not require a word dictionary",
  "喜欢 与 AI 都被拆开，sequence 较长": "喜欢 and AI are split, producing a longer sequence",
  "word-like（指定 segmenter）": "Word-like (specified segmenter)",
  "[我, 喜欢, AI, ，, AI, 也, 喜欢, 猫, 。]": "[我, 喜欢, AI, ，, AI, 也, 喜欢, 猫, 。]",
  "短而且容易阅读": "Short and easy to read",
  "中文边界、未见词和规范差异需要额外规则": "Chinese word boundaries, unseen words, and normalization require additional rules",
  "固定 256 个 base symbols 可覆盖任意有效 UTF-8 文本": "A fixed set of 256 byte symbols covers valid UTF-8 text",
  "最长；单个 byte fragment 不一定可独立阅读": "Longest in this example; a byte fragment may not be independently readable",
  "subword（进一步训练后的示意）": "Subword (illustrative result after additional training)",
  "常见片段短，少见文本仍可拆成更小单位": "Frequent pieces can use fewer tokens; less familiar text can be split into smaller units",
  "merge、normalization 与 ID 都属于该 tokenizer 版本": "Merges, normalization, and ID assignments belong to the tokenizer version",
  "这些长度分别属于四种 tokenizer rule，不能把它们当成同一单位的四次测量。": "These lengths come from different tokenization rules. They are not four measurements using the same unit.",
  "Token count 既不是 character count，也不是 word count": "Token count is neither character count nor word count in general",
  "模型只看到 tokenizer 发出的 positions。真实 tokenizer 还可能特殊处理 leading spaces、normalization 或 byte fallback，因此另一 tokenizer 对这句文字可能得到完全不同的 L。": "The model processes the positions emitted by its tokenizer. Leading spaces, normalization, and byte fallback can all change segmentation, so another tokenizer may give a different L for this sentence.",
  "Unicode code point 与 UTF-8 byte 是不同单位。": "Unicode code points and UTF-8 bytes are different units.",
  "中文没有空格不表示它天然只有一种 word segmentation。": "The absence of spaces in Chinese does not imply one uniquely determined word segmentation.",
  "跨 tokenizer 比较 L 时，必须先承认每个 position 的单位不同。": "When comparing L across tokenizers, acknowledge that their position units differ.",
  "13 个 Unicode code points 是否证明所有 tokenizer 都会产生 13 个 IDs？": "Does a sentence with thirteen code points necessarily produce thirteen token IDs?",
  "不会。character rule 在这里产生 13 个单位；word-like 与示意 subword 各产生 9 个，byte rule 产生 31 个。L 由 tokenizer artifact 决定。": "No. This character rule gives thirteen units, the word-like and illustrative subword rules give nine each, and the byte rule gives thirty-one. L depends on the tokenizer artifact.",
  "2. 为什么不简单按 Word 切：覆盖与长度的两难": "2. Why not use whole words? Coverage versus length",
  "whole-word Vocabulary 若存下所有拼写、词形、专名和组合会迅速膨胀；若只保留有限清单，又必须处理清单外文字。": "A vocabulary containing every spelling, word form, name, and compound would grow rapidly. A finite whole-word list instead needs a policy for text outside that list.",
  "用 subword 解释“常见片段短、少见片段可分解”的工程折中。": "Explain subwords as a practical compromise: short encodings for frequent pieces, decomposition for other combinations.",
  "把 vocabulary size V、sequence length L 与 Attention 的位置成本连起来。": "Connect vocabulary size V and sequence length L to attention's position-related cost.",
  "在固定句“我喜欢AI，AI也喜欢猫。”中，word-like tokenizer 可把 喜欢 与 AI 保持为整体；byte base 则一定能表达这些有效 UTF-8 bytes，但需要 31 个 positions。Subword 从覆盖能力较强的小单位出发，再把训练语料中的常见相邻片段合并。": "For the fixed Chinese sentence, a word-like tokenizer can keep 喜欢 and AI intact. A byte vocabulary covers its valid UTF-8 encoding but needs thirty-one positions. Subword schemes can start from smaller units and learn frequent adjacent combinations from a corpus.",
  "主要代价": "Main trade-off",
  "需要很多完整词；缺项时常依赖 <UNK> 或额外 fallback": "Many whole-word entries; missing words need <UNK> or another fallback",
  "常见词通常较短": "Common words can be represented compactly",
  "V 大或 unknown information loss": "A large V or information loss on unknown words",
  "base V 较小；byte base 可覆盖有效 UTF-8": "A smaller base vocabulary; a complete byte base covers valid UTF-8 text",
  "固定句为 13 / 31": "Thirteen / thirty-one positions for this sentence",
  "更多 positions 与更弱的人类可读性": "More positions and, for bytes, reduced direct readability",
  "通常介于两端；本示意为 9": "Often a compromise; nine positions in this illustration",
  "需要冻结训练出的 merges 与 routing rules": "Learned merges and segmentation rules must be saved and fixed for use",
  "在宽度 C 固定时，更长的 token sequence 会增加 Attention 的位置对计算；这不是整个模型成本的完整公式。": "At a fixed width C, longer token sequences increase attention's position-pair calculations. That is not the complete cost formula for the whole model.",
  "辅助 morphology 例子不是保证": "A morphology illustration is not a guarantee",
  "某个假想英文 Vocabulary 可能把 unhappiness 切成 [un, happi, ness]，但 BPE 不保证得到语言学 morphemes，也不因完成这种切分就理解该词。": "A hypothetical English vocabulary might split unhappiness into [un,happi,ness]. BPE does not guarantee linguistic morphemes, nor does producing that split demonstrate understanding of the word.",
  "Subword 不保证边界与 morpheme 或语义一致。": "Subword boundaries need not align with morphemes or meanings.",
  "BPE 让新字符串可分解，不代表模型已知道新实体。": "Encoding a new string in known pieces does not mean the model knows the new entity.",
  "word-level tokenizer 也可以用 <UNK>；代价是多个 unknown pieces 会坍缩到同一地址。": "A word-level tokenizer can use <UNK>, but distinct unknown strings then collapse to one ID.",
  "Subword 相比有限 whole-word Vocabulary 解决了什么问题？": "What can subwords do that a finite whole-word vocabulary alone cannot?",
  "它能用已知的较小 pieces 表示未作为完整 entry 出现的组合，不必为每个 whole word 新增一行；是否理解这些组合仍由模型训练决定。": "They can represent combinations using known smaller pieces without requiring a new entry for every whole word. Coverage still depends on the base symbols and fallback policy; understanding depends on the trained model.",
  "3. Vocabulary Size 的 Trade-off：V 与 L 会同时改变系统": "3. Vocabulary-size trade-offs: V and L both affect the system",
  "增大 Vocabulary 往往能缩短文字序列，却也扩大 input/output 两个 vocabulary-facing interfaces；只谈一端会误判参数和计算成本。": "A larger vocabulary can shorten some encodings but enlarges the vocabulary-facing input/output interfaces. Considering only one side misses part of the cost.",
  "说明 V 决定可选 token rows/columns，L 与模型 context T 决定一次能覆盖多少原文。": "V determines vocabulary candidates and lookup entries; L and context size T determine how much original text a model input spans.",
  "区分 generic biased untied head 的 2VC+V 与后续 canonical MiniGPT 的 bias-free untied 2VC。": "Distinguish 2VC+V for separate embedding/output weights with an output bias from 2VC for the later separate, bias-free reference model.",
  "固定句的 13、9、31 positions 只显示方向，不证明“9 最优”。较大的 V 可以为常见片段分配单独 entry，较小的 V 则常要用更多 positions 表达同一文字。模型仍会在每个 position 输出 V 个 logits。": "The sentence's lengths of thirteen, nine, and thirty-one illustrate different choices; they do not prove nine is optimal. Larger vocabularies can dedicate entries to common pieces, while smaller ones may use more positions. The model still produces V logits at every processed position.",
  "较少 rows 与 output candidates": "Fewer embedding rows and output candidates",
  "往往更大 L": "Often a larger L",
  "固定 T 能覆盖的原文可能更少，位置对更多": "A fixed T may cover less original text; a longer processed sequence has more position pairs",
  "较多 rows 与 output candidates": "More embedding rows and output candidates",
  "常见片段往往更短": "Frequent pieces may need fewer positions",
  "每一步的 LM-head computation / memory 增大": "More output-head computation and storage per position",
  "输入 embedding 有 V 行、每行 C 个 features；输出在每个 batch/time position 给 V 个候选 logits。": "The input embedding has V rows of C features. The output scores V candidates at each batch/sequence position.",
  "只有 generic 的 untied 且带 bias LM head 变体才包含额外 V 个 bias 参数。": "The extra V biases apply only to the illustrated separate-weight output head with a bias.",
  "Week 10 的 canonical MiniGPT 使用 bias-free、untied LM head，因此 vocabulary-facing 参数为 2VC。": "Week 10's reference MiniGPT uses separate embedding/output weights and no LM-head bias, so these two tables contribute 2VC parameters.",
  "这里只计算两张 vocabulary-facing weight tables；不包含 Transformer blocks，也不声称这是某个真实模型配置": "Only the two vocabulary-facing tables are counted here, not Transformer blocks. These are illustrative configurations.",
  "本章教学尺寸 V=11, C=4": "Teaching dimensions V=11, C=4",
  "示意规模 V=50,000, C=768": "Illustrative larger dimensions V=50,000, C=768",
  "这个教学 batch 的每个 row、每个 time position 都要为 11 个候选产生分数，因此共有 88 个 logits；这与上表恰好同为 88 只是数值巧合。": "This teaching batch produces scores for eleven candidates at every position: eighty-eight logits in total. Matching the eighty-eight parameter count above is a numerical coincidence, not a relationship between activations and parameters.",
  "现在可以看到 V 的双重作用：它一方面决定 embedding / LM-head 的表有多宽，另一方面决定每个 position 要比较多少个 next-token candidates；而 tokenizer 通过改变 L，又会改变要处理多少个 positions。": "V determines the size of vocabulary-facing parameter tables and the number of candidates scored per position. Through segmentation, the tokenizer also changes L, the number of positions needed to represent text.",
  "Weight tying 只改 parameter accounting": "Weight tying changes sharing, not the number of candidates",
  "若 input embedding 与 LM-head weight 真正共享同一 Parameter，独立参数会减少；但输出仍有 V 个候选。Week 10 的 canonical mini-model 明确采用 untied weights。": "If the input embedding and LM head genuinely share one Parameter, the number of independent parameters decreases and gradients from both uses affect that shared tensor. Output still contains V candidates. Week 10's reference mini-model explicitly keeps the weights separate.",
  "较大 V 不自动更快；短 sequence 与更大 output axis 是不同成本。": "A larger V is not automatically faster. Shorter input sequences and a larger output axis affect different costs.",
  "T 是 context positions，V 是 candidate IDs，C 是每个 position 的 feature width。": "T counts context positions, V vocabulary IDs, and C features per position.",
  "不能把 generic 2VC+V 套到 bias-free canonical MiniGPT 上。": "Do not apply the biased-head count 2VC+V to the bias-free reference model.",
  "C 固定且 V 翻倍，哪两个常见 interfaces 会变大？": "With C fixed, which interfaces grow when V doubles?",
  "input embedding 的 row 数与 LM head 的 vocabulary dimension 都变大。若 weights untied，两张表各自增长；tying 只改变共享参数数量，不改变 V 个 logits。": "The input embedding has more rows and the LM head has more output candidates. Separate weight tables each grow; sharing changes parameter accounting but not the V-logit interface.",
  "4. Character Tokenizer：先看清 Encode / Decode 闭环": "4. A character tokenizer: first complete the encode/decode loop",
  "直接进入 byte-level BPE 会同时遇到 byte、merge 与 special-token policy，反而看不清最基本的文字—ID 双向映射。": "Starting directly with byte BPE introduces bytes, merges, and special-token rules all at once. First isolate the basic text-to-ID mapping.",
  "用一个可手查的 code-point Vocabulary 演示 encode、decode、dtype 与 shape。": "Use a small code-point vocabulary to demonstrate encoding, decoding, dtype, and shape.",
  "把透明的教学 tokenizer 与后面的 w09-readable-v1 ID space 明确分开。": "Keep this transparent example separate from the later w09-readable-v1 ID space.",
  "对“我喜欢AI，AI也喜欢猫。”，这个简化 tokenizer 把每个 Unicode code point 当作 token。它适合检查闭环，却不等于 production 最佳方案，也不能自动处理 Vocabulary 外 code point、grapheme cluster 或 normalization 差异。": "For “我喜欢AI，AI也喜欢猫。”, this tokenizer treats each Unicode code point as a token. It is a useful mapping demonstration, not a production recommendation. It does not automatically handle out-of-vocabulary code points, grapheme clusters, or normalization differences.",
  "import torch\n\ntext = \"我喜欢AI，AI也喜欢猫。\"\ncharacter_tokens = [\"我\", \"喜\", \"欢\", \"A\", \"I\", \"，\", \"也\", \"猫\", \"。\"]\nchar_to_id = {token: index for index, token in enumerate(character_tokens)}\nid_to_char = {index: token for token, index in char_to_id.items()}\n\n\ndef encode_characters(value: str) -> list[int]:\n    return [char_to_id[character] for character in value]\n\n\ndef decode_characters(ids: list[int]) -> str:\n    return \"\".join(id_to_char[index] for index in ids)\n\n\nids = encode_characters(text)\ntoken_tensor = torch.tensor(ids, dtype=torch.long)\n\nassert ids == [0, 1, 2, 3, 4, 5, 3, 4, 6, 1, 2, 7, 8]\nassert tuple(token_tensor.shape) == (13,)\nassert decode_characters(ids) == text": "import torch\n\ntext = \"我喜欢AI，AI也喜欢猫。\"\ncharacter_tokens = [\"我\", \"喜\", \"欢\", \"A\", \"I\", \"，\", \"也\", \"猫\", \"。\"]\nchar_to_id = {token: index for index, token in enumerate(character_tokens)}\nid_to_char = {index: token for token, index in char_to_id.items()}\n\n\ndef encode_characters(value: str) -> list[int]:\n    return [char_to_id[character] for character in value]\n\n\ndef decode_characters(ids: list[int]) -> str:\n    return \"\".join(id_to_char[index] for index in ids)\n\n\nids = encode_characters(text)\ntoken_tensor = torch.tensor(ids, dtype=torch.long)\n\nassert ids == [0, 1, 2, 3, 4, 5, 3, 4, 6, 1, 2, 7, 8]\nassert tuple(token_tensor.shape) == (13,)\nassert decode_characters(ids) == text",
  "这个受限 Vocabulary 中，每个已知 code point 映射为一个整数；固定句形成长度 13 的 torch.long vector。": "Each known code point maps to one integer in this limited vocabulary. The fixed sentence becomes a torch.long vector of length thirteen.",
  "这里的 IDs 不能带到后面的快照": "Do not carry these IDs into the later snapshot",
  "在本节 char_to_id 中，AI 是两个 IDs 3、4；w09-readable-v1 把 AI 作为 ID 6。两组数字分别属于不同 tokenizer artifacts，不能混用。": "Here char_to_id encodes AI as IDs 3 and 4. w09-readable-v1 encodes the whole AI piece as ID 6. The two mappings belong to different tokenizer artifacts.",
  "Python 字符迭代近似展示 code points，不证明每个 user-perceived grapheme 都只有一个 code point。": "Iterating a Python string exposes Unicode code points, not necessarily complete user-perceived characters; a grapheme cluster can contain several code points.",
  "本示例遇到 Vocabulary 外字符会 KeyError；真实 encoder 必须定义 unknown、fallback 或 error policy。": "This example raises KeyError for an unknown code point. A real encoder needs an explicit rejection, unknown-token, or fallback policy.",
  "Character tokenizer 仍要定义 Unicode normalization，不能仅靠一张字典解决所有等价形式。": "Character tokenization still needs a defined normalization policy; a dictionary alone does not resolve equivalent Unicode forms.",
  "为什么明知本句会有 13 个 tokens，仍值得先实现 character tokenizer？": "Why implement the character tokenizer first even though it uses thirteen tokens here?",
  "它让 Vocabulary、integer lookup、encode、decode、torch.long 与 [L] shape 全部可手查，从而暴露所有 tokenizer 共享的基本数据契约。": "It makes vocabulary, integer lookup, encoding, decoding, torch.long, and shape [L] directly inspectable, exposing the basic interface shared by all tokenizers.",
  "8. Tokenizer Training 与 Text Encoding 不同": "8. Tokenizer Training and Text Encoding Are Different",
  "若把“训练 tokenizer”和“编码一个 prompt”混成同一动作，学习者会误以为每次输入“我喜欢AI，AI也喜欢猫。”都会改 Vocabulary 或重新排序 IDs。": "If we treat “train a tokenizer” and “encode a prompt” as the same operation, it is easy to imagine that every new input changes the vocabulary or reorders its IDs. That is not what encoding does.",
  "把离线 artifact construction 与反复应用 frozen artifact 分成两条明确阶段。": "Separate constructing an artifact offline from repeatedly applying that frozen artifact.",
  "规定 from-scratch evaluation 时 tokenizer fitting 只读取 train documents，并分离 encode_content / encode_document。": "For evaluation from scratch, fit the tokenizer on training documents only. Give encode_content and encode_document separate, explicit responsibilities.",
  "是：选择 normalization、base vocab、merges、special IDs": "Yes: choose normalization, the base vocabulary, merges and special IDs",
  "否": "No",
  "否：A 不变；model parameters 改变": "No: A stays fixed; model parameters change",
  "Tokenizer training 产生 artifact A；若从零评估，应先按 document 划分并只用 train documents 拟合 A。": "Tokenizer training produces artifact A. For evaluation from scratch, split the documents first and fit A using only the training documents.",
  "Encoding 只应用 frozen A；不会更新 pair counts、添加 Vocabulary row 或改变 merge order。": "Encoding applies frozen artifact A. It does not update pair counts, add vocabulary rows or change merge order.",
  "下面把 w09-readable-v1 写成一个最小、具体的 teaching artifact，而不是调用未配置的通用 library tokenizer。它使用 identity normalization、显式 ordered Vocabulary，以及固定的 longest-first content routes；无法匹配的一个 Unicode code point 映射为 <UNK>。这个 routing 是教学约定，不声称由前面两轮 toy BPE 直接产生。": "Below, w09-readable-v1 is a small, concrete teaching artifact—not an unspecified library tokenizer. It uses identity normalization, an explicitly ordered vocabulary and fixed longest-first content routes. An unmatched Unicode code point becomes <UNK>. This routing policy is a teaching convention; it is not claimed to be the result of the preceding two toy BPE merges.",
  "这里切换到新的可读 artifact": "We now switch to a separate readable artifact",
  "上一节的 256、257 属于 toy-byte-bpe，现在停止使用。下面为了让 special tokens、document stream 和 batch 能逐项读懂，改用独立的 w09-readable-v1：AI=6、猫=9、V=11。它不是前两轮 BPE 的训练结果，也不能与那套 IDs 混合。": "IDs 256 and 257 belonged to toy-byte-bpe; we stop using that ID space here. To make special tokens, document streams and batches easy to inspect, we now use the independent w09-readable-v1 artifact: AI=6, 猫=9 and V=11. It is not the output of the two BPE rounds, and their IDs must not be mixed.",
  "① TOKENS 冻结 ordered Vocabulary 与 special IDs": "① TOKENS freezes the ordered vocabulary and special IDs",
  "② CONTENT_ROUTES 规定最长优先的内容切分": "② CONTENT_ROUTES defines longest-first content segmentation",
  "③ segment_content 只返回 token strings": "③ segment_content returns token strings only",
  "④ encode_content 把 strings lookup 成 IDs，不添加边界": "④ encode_content looks up their IDs without adding boundaries",
  "⑤ encode_document 恰好添加一次 BOS 与 EOS": "⑤ encode_document adds BOS and EOS exactly once",
  "⑥ decode 按同一 ID table 重建文字": "⑥ decode reconstructs text using the same ID table",
  "第一次阅读下面代码时，只追踪固定句从 cursor=0 到 cursor=len(text) 的移动；第二次再检查 special-token ownership 与 error branches。这样可以先看懂数据流，再看防御性细节。": "On your first reading, follow the cursor through the fixed sentence from 0 to len(text). On the second reading, inspect boundary ownership and error handling. This separates the main data flow from the defensive details.",
  "TOKENS = (\n    \"<BOS>\",\n    \"<EOS>\",\n    \"<PAD>\",\n    \"<UNK>\",\n    \"我\",\n    \"喜欢\",\n    \"AI\",\n    \"，\",\n    \"也\",\n    \"猫\",\n    \"。\",\n)\nTOKEN_TO_ID = {token: token_id for token_id, token in enumerate(TOKENS)}\nID_TO_TOKEN = {token_id: token for token, token_id in TOKEN_TO_ID.items()}\n\nBOS_ID = TOKEN_TO_ID[\"<BOS>\"]  # 0\nEOS_ID = TOKEN_TO_ID[\"<EOS>\"]  # 1\nPAD_ID = TOKEN_TO_ID[\"<PAD>\"]  # 2\nUNK_ID = TOKEN_TO_ID[\"<UNK>\"]  # 3\n\n# Deterministic longest-first routing for this small teaching artifact.\n# Equal-length routes retain this declared order.\nCONTENT_ROUTES = (\"喜欢\", \"AI\", \"我\", \"，\", \"也\", \"猫\", \"。\")\nHIDDEN_ON_DECODE_IDS = {BOS_ID, EOS_ID, PAD_ID}\n\n\nclass ReadableTokenizerV1:\n    version = \"w09-readable-v1\"\n    normalization = \"identity\"\n    vocabulary = TOKENS\n\n    def segment_content(self, text: str) -> list[str]:\n        pieces: list[str] = []\n        cursor = 0\n        while cursor < len(text):\n            matched = next(\n                (\n                    piece\n                    for piece in CONTENT_ROUTES\n                    if text.startswith(piece, cursor)\n                ),\n                None,\n            )\n            if matched is None:\n                pieces.append(\"<UNK>\")\n                cursor += 1\n            else:\n                pieces.append(matched)\n                cursor += len(matched)\n        return pieces\n\n    def encode_content(\n        self,\n        text: str,\n        *,\n        add_special_tokens: bool = False,\n    ) -> list[int]:\n        if add_special_tokens:\n            raise ValueError(\"encode_content never adds BOS/EOS\")\n        return [TOKEN_TO_ID[piece] for piece in self.segment_content(text)]\n\n    def encode_document(\n        self,\n        text: str,\n        *,\n        add_special_tokens: bool = True,\n    ) -> list[int]:\n        if not add_special_tokens:\n            raise ValueError(\"use encode_content when boundaries are unwanted\")\n        return [\n            BOS_ID,\n            *self.encode_content(text, add_special_tokens=False),\n            EOS_ID,\n        ]\n\n    def decode(\n        self,\n        ids: list[int],\n        *,\n        skip_special_tokens: bool = True,\n    ) -> str:\n        pieces: list[str] = []\n        for token_id in ids:\n            if token_id not in ID_TO_TOKEN:\n                raise ValueError(f\"token ID out of range: {token_id}\")\n            if skip_special_tokens and token_id in HIDDEN_ON_DECODE_IDS:\n                continue\n            pieces.append(ID_TO_TOKEN[token_id])\n        return \"\".join(pieces)\n\n\nW09_READABLE_V1 = ReadableTokenizerV1()\n\n\ndef encode_content(text: str) -> list[int]:\n    return W09_READABLE_V1.encode_content(\n        text,\n        add_special_tokens=False,\n    )\n\n\ndef encode_document(text: str) -> list[int]:\n    return W09_READABLE_V1.encode_document(\n        text,\n        add_special_tokens=True,\n    )\n\n\nRUNNING_TEXT = \"我喜欢AI，AI也喜欢猫。\"\ncontent_ids = encode_content(RUNNING_TEXT)\ndocument_ids = encode_document(RUNNING_TEXT)\n\nassert content_ids == [4, 5, 6, 7, 6, 8, 5, 9, 10]\nassert document_ids == [0, *content_ids, 1]\nassert W09_READABLE_V1.decode(\n    document_ids,\n    skip_special_tokens=True,\n) == RUNNING_TEXT\n\n# These calls never mutate vocabulary, routes, or IDs.": "TOKENS = (\n    \"<BOS>\",\n    \"<EOS>\",\n    \"<PAD>\",\n    \"<UNK>\",\n    \"我\",\n    \"喜欢\",\n    \"AI\",\n    \"，\",\n    \"也\",\n    \"猫\",\n    \"。\",\n)\nTOKEN_TO_ID = {token: token_id for token_id, token in enumerate(TOKENS)}\nID_TO_TOKEN = {token_id: token for token, token_id in TOKEN_TO_ID.items()}\n\nBOS_ID = TOKEN_TO_ID[\"<BOS>\"]  # 0\nEOS_ID = TOKEN_TO_ID[\"<EOS>\"]  # 1\nPAD_ID = TOKEN_TO_ID[\"<PAD>\"]  # 2\nUNK_ID = TOKEN_TO_ID[\"<UNK>\"]  # 3\n\n# Deterministic longest-first routing for this small teaching artifact.\n# Equal-length routes retain this declared order.\nCONTENT_ROUTES = (\"喜欢\", \"AI\", \"我\", \"，\", \"也\", \"猫\", \"。\")\nHIDDEN_ON_DECODE_IDS = {BOS_ID, EOS_ID, PAD_ID}\n\n\nclass ReadableTokenizerV1:\n    version = \"w09-readable-v1\"\n    normalization = \"identity\"\n    vocabulary = TOKENS\n\n    def segment_content(self, text: str) -> list[str]:\n        pieces: list[str] = []\n        cursor = 0\n        while cursor < len(text):\n            matched = next(\n                (\n                    piece\n                    for piece in CONTENT_ROUTES\n                    if text.startswith(piece, cursor)\n                ),\n                None,\n            )\n            if matched is None:\n                pieces.append(\"<UNK>\")\n                cursor += 1\n            else:\n                pieces.append(matched)\n                cursor += len(matched)\n        return pieces\n\n    def encode_content(\n        self,\n        text: str,\n        *,\n        add_special_tokens: bool = False,\n    ) -> list[int]:\n        if add_special_tokens:\n            raise ValueError(\"encode_content never adds BOS/EOS\")\n        return [TOKEN_TO_ID[piece] for piece in self.segment_content(text)]\n\n    def encode_document(\n        self,\n        text: str,\n        *,\n        add_special_tokens: bool = True,\n    ) -> list[int]:\n        if not add_special_tokens:\n            raise ValueError(\"use encode_content when boundaries are unwanted\")\n        return [\n            BOS_ID,\n            *self.encode_content(text, add_special_tokens=False),\n            EOS_ID,\n        ]\n\n    def decode(\n        self,\n        ids: list[int],\n        *,\n        skip_special_tokens: bool = True,\n    ) -> str:\n        pieces: list[str] = []\n        for token_id in ids:\n            if token_id not in ID_TO_TOKEN:\n                raise ValueError(f\"token ID out of range: {token_id}\")\n            if skip_special_tokens and token_id in HIDDEN_ON_DECODE_IDS:\n                continue\n            pieces.append(ID_TO_TOKEN[token_id])\n        return \"\".join(pieces)\n\n\nW09_READABLE_V1 = ReadableTokenizerV1()\n\n\ndef encode_content(text: str) -> list[int]:\n    return W09_READABLE_V1.encode_content(\n        text,\n        add_special_tokens=False,\n    )\n\n\ndef encode_document(text: str) -> list[int]:\n    return W09_READABLE_V1.encode_document(\n        text,\n        add_special_tokens=True,\n    )\n\n\nRUNNING_TEXT = \"我喜欢AI，AI也喜欢猫。\"\ncontent_ids = encode_content(RUNNING_TEXT)\ndocument_ids = encode_document(RUNNING_TEXT)\n\nassert content_ids == [4, 5, 6, 7, 6, 8, 5, 9, 10]\nassert document_ids == [0, *content_ids, 1]\nassert W09_READABLE_V1.decode(\n    document_ids,\n    skip_special_tokens=True,\n) == RUNNING_TEXT\n\n# These calls never mutate vocabulary, routes, or IDs.",
  "encoding 与 decoding 都只应用 frozen artifact，不产生新 Vocabulary entry": "Encoding and decoding apply the frozen artifact; neither creates a new vocabulary entry.",
  "[我,喜欢,AI,，,AI,也,喜欢,猫,。]": "[我,喜欢,AI,，,AI,也,喜欢,猫,。]",
  "encode 不是 gradient descent，也不会根据当前 prompt 学新 merge。": "Encoding is not gradient descent. It does not learn a new merge from the current prompt.",
  "Transformer optimizer 更新 model parameters，不更新 frozen tokenizer artifact。": "The Transformer optimizer updates model parameters, not the frozen tokenizer artifact.",
  "若 tokenizer 在 validation documents 上重新 fitting，会改变评估协议并可能泄漏信息。": "Refitting the tokenizer on validation documents changes the evaluation protocol and can leak information.",
  "encode_content 与 encode_document 的 boundary ownership 必须清楚，不能重复添加 BOS/EOS。": "Keep boundary ownership clear: encode_content and encode_document must not both add BOS/EOS.",
  "固定 BPE rule (41,49)→256 后，再 encode 固定句会改变这条 rule 吗？": "After freezing the BPE rule (41,49)→256, does encoding the fixed sentence again change that rule?",
  "不会。Encoding 只按保存的 ordered merges 应用规则；改变 merge 需要产生新 tokenizer version，并同步改变与之绑定的 model interfaces。": "No. Encoding applies the saved ordered merges. Changing a merge requires a new tokenizer version and compatible model interfaces.",
  "9. Tokenizer 与 Model 必须绑定：Shape 相同仍可能完全错位": "9. Bind the Tokenizer to the Model: Matching Shapes Are Not Enough",
  "模型的 embedding row 与输出 logit column 是在特定 ID mapping 下训练的。换一个 tokenizer 后，即使 V 恰好相同，合法整数也可能查到错误的 token 参数。": "The model's embedding rows and output-logit entries were trained under a particular ID mapping. With a different tokenizer, even an in-range integer may select parameters for the wrong token, despite an unchanged vocabulary size.",
  "定义 checkpoint 的 tokenizer compatibility contract，而不只检查 tensor dimensions。": "Define a tokenizer-compatibility contract for checkpoints, not just tensor-dimension checks.",
  "用 w09-readable-v1 中 AI / 喜欢 的 ID swap 显示静默错位。": "Swap the IDs of AI and 喜欢 in w09-readable-v1 to expose a silent mismatch.",
  "主线先保持简单：沿用 Week 6 的编码器": "Keep the main learning path simple: reuse the Week 6 encoder",
  "本课必修计算仍使用五词表；实现放在 course_examples/course_data.py。先运行 python week09_data_protocol.py，它会打印同一份 inputs/targets、一次未知词报错说明，以及文档窗口的边界。本节后面的 w09-readable-v1 是有特殊 token 的比较实验，不接替主线词表。": "The core calculations still use the five-token vocabulary implemented in course_examples/course_data.py. Start with python week09_data_protocol.py: it prints the same inputs/targets, demonstrates an unknown-token error and shows document-window boundaries. The later w09-readable-v1 comparison adds special tokens; it does not replace the main vocabulary.",
  "按空格切分的 encode 不学习参数，也不改变词表。“我   喜欢”与“我 喜欢”会编码相同；decode 返回规范的单空格文本，不能声称保留任意原始空白。字符版则逐字符保留本实验允许的原文。每个往返保证都要说明范围。": "Whitespace-based encoding neither learns parameters nor changes the vocabulary. For example, “you   like” and “you like” encode identically; decoding returns canonical single-space text, not the original spacing. The character version preserves individual characters within its supported alphabet. Every round-trip guarantee needs an explicit scope.",
  "w09-readable-v1 把“我喜欢AI，AI也喜欢猫。”中的 喜欢 编为 5、AI 编为 6。若另一个同样 V=11 的 tokenizer 把两者交换，输入 6 会读取原本为 AI 训练的 embedding row，却被新系统解释成 喜欢；输出 column 6 也会 decode 成错误 piece。": "In w09-readable-v1, 喜欢 has ID 5 and AI has ID 6. If another V=11 tokenizer swaps those IDs, input 6 still reads the embedding row trained for AI, but the new system calls it 喜欢. Output entry 6 will also decode to the wrong piece.",
  "Tokenizer artifact 与 model checkpoint 是一个兼容性 bundle": "The tokenizer artifact and model checkpoint form a compatibility bundle",
  "定义每个 embedding row 与 LM-head column 的名称": "Names the token represented by every embedding row and output-logit entry",
  "决定新文字如何组合成 Vocabulary entries": "Determines how new text is assembled into vocabulary entries",
  "决定 merge 前看到的 symbols 与 boundaries": "Determines the symbols and boundaries seen before merging",
  "定义 BOS、EOS、PAD、UNK 的控制地址与何时出现": "Defines the control IDs for BOS, EOS, PAD and UNK, and when they appear",
  "在加载前做精确 compatibility check": "Supports an exact compatibility check before loading",
  "声明 V、C、context 等 shape，并保存已学习参数": "Declares shapes such as V, C and context length, and stores learned parameters",
  "w09-readable-v1:\nID 5 -> 喜欢\nID 6 -> AI\n\nincompatible-tokenizer:\nID 5 -> AI\nID 6 -> 喜欢\n\nBoth report V=11.\nModel input ID 6 is numerically in range, but its learned row and displayed token disagree.": "w09-readable-v1:\nID 5 -> 喜欢\nID 6 -> AI\n\nincompatible-tokenizer:\nID 5 -> AI\nID 6 -> 喜欢\n\nBoth report V=11.\nModel input ID 6 is numerically in range, but its learned row and displayed token disagree.",
  "Compatibility 不只是 V 相等": "Compatibility means more than equal V",
  "安全加载应验证 tokenizer policy/version/hash、ordered tokens、special IDs 与 model config。给 Vocabulary 追加 entry 还需要调整受 V 影响的 embedding 与 LM head，并通常需要相应训练。": "Safe loading checks tokenizer policy/version/hash, ordered tokens, special IDs and model configuration. Adding vocabulary entries also requires resizing the vocabulary-dependent embedding and output head, and usually training the new entries.",
  "两个 tokenizer 包含相同 token strings 但顺序不同，仍然不兼容。": "Two tokenizers with the same token strings in different orders are still incompatible.",
  "Normalization policy 改变可能在 lookup 之前就产生另一串 IDs。": "A normalization change can produce different IDs before lookup even begins.",
  "不能只 resize 一张表就假定新 token 已经学会有用表示。": "Resizing a table does not mean its new tokens have learned useful representations.",
  "为什么两个 V=11 的 tokenizer 仍可能不能共享同一个 checkpoint？": "Why might two V=11 tokenizers be incompatible with the same checkpoint?",
  "V 只说明 row/column 数量；ordered mapping、merges、normalization 与 special policy 可能不同。只要某个 ID 改名，已训练参数的地址语义就错位。": "V tells us only how many token entries there are. Ordered mappings, merges, normalization and special-token policies can differ. Renaming an ID changes the meaning of the address used by the learned parameters.",
  "12. 把 Corpus 变成 Token Tensor：保留顺序与文档边界": "12. Turn a Corpus into a Token Tensor: Keep Order and Document Boundaries",
  "Corpus 是多份原始 documents；模型训练需要可复现的一维 integer stream。若随意 shuffle 单个 tokens 或重复插入边界，next-token context 就被破坏。": "A corpus contains raw documents; training needs a reproducible integer stream. Shuffling individual tokens or inserting boundaries twice breaks the intended next-token contexts.",
  "用 frozen tokenizer 按 document 编码，再依次串接为 [L] stream。": "Encode documents using the frozen tokenizer, then concatenate the chosen sequences into a stream of shape [L].",
  "让 encode_document 独自负责 BOS/EOS，并明确 torch.long 是 embedding lookup 的输入 dtype。": "Let encode_document own BOS/EOS insertion. Use torch.long for the embedding-lookup inputs in this course.",
  "只有一份固定文档“我喜欢AI，AI也喜欢猫。”时，w09-readable-v1 直接得到 [0,4,5,6,7,6,8,5,9,10,1]，shape [11]。它仍是一条 chronological stream，不是 batch，也没有 C feature axis。": "For the single document “我喜欢AI，AI也喜欢猫。”, w09-readable-v1 produces [0,4,5,6,7,6,8,5,9,10,1], shape [11]. This is still one ordered stream—not a batch, and not a tensor with a C feature axis.",
  "import torch\n\nfrom w09_readable_v1 import encode_document\n\ndocuments = [\"我喜欢AI，AI也喜欢猫。\"]\n\n# encode_document already inserts exactly one BOS and one EOS per document.\nstream: list[int] = []\nfor document in documents:\n    stream.extend(encode_document(document))\n\nstream_tensor = torch.tensor(stream, dtype=torch.long)\n\nassert stream == [0, 4, 5, 6, 7, 6, 8, 5, 9, 10, 1]\nassert tuple(stream_tensor.shape) == (11,)\nassert stream_tensor.dtype == torch.long": "import torch\n\nfrom w09_readable_v1 import encode_document\n\ndocuments = [\"我喜欢AI，AI也喜欢猫。\"]\n\n# encode_document already inserts exactly one BOS and one EOS per document.\nstream: list[int] = []\nfor document in documents:\n    stream.extend(encode_document(document))\n\nstream_tensor = torch.tensor(stream, dtype=torch.long)\n\nassert stream == [0, 4, 5, 6, 7, 6, 8, 5, 9, 10, 1]\nassert tuple(stream_tensor.shape) == (11,)\nassert stream_tensor.dtype == torch.long",
  "每份 document sequence 已包含约定边界，按 document order 串接后得到一维 stream。": "Each document sequence already has its agreed boundaries. Concatenating those sequences in document order produces a one-dimensional stream.",
  "边界 token 让模型看见边界，但是否允许跨边界 Attention 或 loss 仍是独立的数据管线决策": "Boundary tokens identify boundaries; whether attention or loss crosses them is a separate data-pipeline decision.",
  "会训练哪种 transition": "Which transitions are supervised?",
  "连续串接 boundary tokens": "Concatenate boundary-marked documents",
  "…内容,<EOS>,<BOS>,下一篇…": "…content,<EOS>,<BOS>,next document…",
  "内容末尾→EOS，也可能包含 EOS→BOS": "Last content token→EOS, and possibly EOS→BOS",
  "实现简单；需要接受控制 token 间的跨文档 transition": "Simple implementation; includes the selected cross-document control-token transitions",
  "每篇文档内单独切 windows": "Build windows separately within each document",
  "window 不跨 document": "No window crosses a document boundary",
  "只监督同一文档内与内容末尾→EOS": "Only within-document transitions, including final content→EOS",
  "语义边界清楚；短尾部可能浪费 positions": "Clear boundaries; short tails may leave positions unused",
  "一张矩形 batch 放多篇文档": "Pack multiple documents into one rectangular batch",
  "用 attention/loss policy 阻断不需要的跨文档连接": "Use explicit attention/loss policies to block unwanted cross-document connections",
  "利用率高，但实现与审计更复杂": "Efficient use of positions, but more complex to implement and audit",
  "本章单文档 stream 不会触发上述差异。扩展到多文档时，必须在数据规范中选定一种 policy；不能只插入 EOS 就声称所有跨文档信息流都已经被阻断。": "These alternatives do not differ for this chapter's single-document stream. With multiple documents, choose and document a policy. Inserting EOS alone does not block all cross-document information flow.",
  "另一种合法 API，但不能混合使用": "Another valid API—but do not mix boundary owners",
  "若 pipeline 选择 encode_content，则可显式构造 [BOS, *content_ids, EOS]；若选择 encode_document，就不得再手动添加。关键是每个 document 的 boundary ownership 唯一且可测试。": "If the pipeline uses encode_content, it can explicitly build [BOS, *content_ids, EOS]. If it uses encode_document, it must not add those boundaries again. Each document needs one clearly defined owner of boundary insertion.",
  "ID tensor 必须是 integer / torch.long；float32 ID 不能作为普通 embedding indices。": "Use integer IDs—torch.long in these examples. float32 values are not valid indices for ordinary embedding lookup.",
  "按 token 随机 shuffle 会摧毁原始 next-token adjacency。": "Randomly shuffling individual tokens destroys the original next-token adjacency.",
  "忘记 document boundary 可能让一个 document 的最后 token 直接预测下一篇无关文档的首 token。": "Without a document-boundary policy, the final token of one document may be trained to predict the first token of an unrelated document.",
  "插入 EOS/BOS 能显式标记边界，但不会自动阻止 window、Attention 或 loss 跨越它。": "EOS/BOS marks a boundary; it does not automatically prevent a window, attention or loss from crossing it.",
  "[L]=[11] 不是 [B,T]，也不是 embedding [11,C]。": "[L]=[11] is neither [B,T] nor an embedding tensor [11,C].",
  "固定句刚变为 stream_tensor 时 shape 与 dtype 是什么？": "What are the shape and dtype immediately after the fixed sentence becomes stream_tensor?",
  "shape 是 [L]=[11]，dtype 是 torch.long。它是一条有序 ID stream，尚未切 window 或 stack batch。": "The shape is [L]=[11] and the dtype is torch.long. It is one ordered ID stream, not yet sliced into windows or stacked into a batch.",
  "13. Train / Validation Split：先隔离 Documents，再切 Windows": "13. Train/Validation Split: Separate Documents Before Building Windows",
  "若把相邻 token windows 随机分到 train 与 validation，几乎重复的上下文可能同时出现在两边，让 validation loss 过度乐观。": "If adjacent windows are randomly assigned to training and validation, nearly identical contexts may appear in both sets. Validation loss can then look misleadingly good.",
  "在 model fitting 之前按 document 或合适的时间/group boundary 分割原始数据。": "Split raw data by document—or an appropriate time/group boundary—before fitting the model.",
  "若 tokenizer 从零训练，只在 train documents 上 fit，然后用同一 frozen artifact 编码两边。": "If training a tokenizer from scratch, fit it on training documents only, then encode both splits with the same frozen artifact.",
  "单句“我喜欢AI，AI也喜欢猫。”只用于 pipeline 手算，不能冒充有意义的 train/validation experiment。真实 corpus 应先分 documents，再冻结 tokenizer，最后分别建立 train_stream 与 val_stream；validation forward 不调用 backward 或 optimizer.step。": "The single sentence “我喜欢AI，AI也喜欢猫。” is a pipeline example, not a meaningful train/validation experiment. For a real corpus, split documents first, freeze the tokenizer, then build train_stream and val_stream separately. Validation forward passes do not call backward or optimizer.step.",
  "Train 与 validation 的 source-document provenance 不相交；每个 window instance 只在所属 document 的 lane 内产生。": "Training and validation have disjoint source-document provenance. Each window instance is built only within its document's assigned split.",
  "这是 provenance guarantee，不是 value-level deduplication：两份独立 documents 可能都包含常见短语或 boilerplate，因此完全相同的 ID window values 可以自然地分别出现在 train 与 validation。若任务还要求去重，必须另外声明 document/group deduplication policy。": "This is a provenance guarantee, not value-level deduplication. Independent documents can contain common phrases or boilerplate, so identical ID windows may appear in both splits. If deduplication is required, specify a separate document/group deduplication policy.",
  "Validation 虽不参与 backward，却会通过选模型、调超参数影响你的决策。若反复针对同一 validation 改进，再把它当完全没见过的最终测验，会过度乐观。正式比较可另留 test split，最后才使用；本课三句玩具数据不足以支持这种质量结论。": "Validation does not participate in backward, but it influences model selection and hyperparameter choices. Repeatedly improving against one validation set makes it less suitable as an untouched final assessment. Formal comparisons can reserve a test split for the end. This course's tiny toy data cannot support strong quality claims.",
  "不能训练完 model 后才从同一 windows 挑一部分叫 validation。": "Do not train on windows first and then relabel some of those same windows as validation.",
  "从零训练 tokenizer 时，默默在 validation text 上 fit merges 会污染 protocol。": "When fitting a tokenizer from scratch, silently learning merges from validation text contaminates the protocol.",
  "Document split 不保证两边绝不会出现数值相同的 token sequence；它保证 source provenance 分离。": "A document split separates provenance; it does not guarantee that the two splits contain no identical token-value sequences.",
  "train loss 下降不能替代 held-out validation。": "Falling training loss is not a substitute for held-out validation.",
  "使用 pretrained tokenizer 时应锁定外部 artifact，而不是在本 corpus 上偷偷重训。": "When using a pretrained tokenizer, pin its external artifact rather than silently retraining it on this corpus.",
  "为什么应在 stream concatenation 之前按 documents 分割？": "Why split by document before concatenating streams?",
  "这样同一 source document 与同一个 window instance 不会跨进两个 splits，也能在每条 lane 内独立保留 document boundaries；不同 documents 仍可能独立产生相同的 token-value sequence。": "This keeps each source document and window instance within one split and lets each split retain its own boundaries. Different documents can still independently contain the same sequence of token values.",
  "14. 从 Token Stream 取一个 Example：Target 必须右移一位": "14. Take One Example from a Stream: Shift Targets One Position Right",
  "若 input 与 target 完全相同，模型学习的是复制当前 ID，而不是预测 stream 中的下一个 ID；若起点越界，target 还会少一个元素。": "If input and target are identical, the model is taught to copy the current ID, not predict the next ID in the stream. An out-of-range start can also leave the target one element short.",
  "从 w09-readable-v1 的 [L]=[11] stream 推导一个 T=4 teacher-forced example。": "Build a teacher-forced example with T=4 from the w09-readable-v1 stream of shape [L]=[11].",
  "逐位置标明 x、y 与 causal left context，避免 shape 正确但监督错位。": "Label each position's x, y and visible left context. Matching shapes alone do not prove that supervision is aligned.",
  "固定 stream s=[0,4,5,6,7,6,8,5,9,10,1] 来自“我喜欢AI，AI也喜欢猫。”。选择 start i=0 与 context length T=4；x 读四个连续 IDs，y 从下一个位置读同样四个 IDs。": "The fixed stream s=[0,4,5,6,7,6,8,5,9,10,1] encodes “我喜欢AI，AI也喜欢猫。”. Choose start i=0 and context length T=4. x reads four consecutive IDs; y reads four IDs beginning one position later.",
  "w09-readable-v1 的完整一维 stream s，L=11": "The full one-dimensional w09-readable-v1 stream s, L=11",
  "x = s[0:4] = [0,4,5,6] = [<BOS>, 我, 喜欢, AI]  shape [T]=[4]\ny = s[1:5] = [4,5,6,7] = [我, 喜欢, AI, ，]       shape [T]=[4]": "x = s[0:4] = [0,4,5,6] = [<BOS>, 我, 喜欢, AI]  shape [T]=[4]\ny = s[1:5] = [4,5,6,7] = [我, 喜欢, AI, ，]       shape [T]=[4]",
  "四个 aligned next-token tasks；target 不是提前提供给同一 query": "Four aligned next-token tasks; a query is not given its target in advance.",
  "[<BOS>, 我]": "[<BOS>, 我]",
  "[<BOS>, 我, 喜欢]": "[<BOS>, 我, 喜欢]",
  "[<BOS>, 我, 喜欢, AI]": "[<BOS>, 我, 喜欢, AI]",
  "Input window 与 target window 从同一 stream 取得，target 的 start 恰好右移一位。": "Input and target windows come from the same stream. The target window starts exactly one position later.",
  "合法 start 必须为 target 保留额外一个 ID；这里共有 L−T=7 个 starts。": "A valid start must leave one extra ID for the final target. Here there are L−T=7 valid starts.",
  "y=x 会把 next-token supervision 写错。": "Setting y=x defines the wrong next-token supervision.",
  "i=L−T=7 会使 y 少一个元素；最后合法 start 是 6。": "i=L−T=7 leaves y one element short. The last valid start is 6.",
  "Transformer position t 可用 permitted left prefix，不只是当前单个 input token。": "At position t, a Transformer can use the permitted left prefix, not just the current input token.",
  "Causal mask 防止 future leakage；shift 则定义正确 label，两者职责不同。": "The causal mask prevents future leakage; the shift defines the correct labels. These are different jobs.",
  "在 i=0 的 example 中，input ID 5（喜欢）对应哪个 target？": "In the i=0 example, which target corresponds to input ID 5 (喜欢, “like”)?",
  "它位于 t=2，对齐的 target 是 ID 6（AI）；模型可用 [<BOS>,我,喜欢] 预测 AI，但不能先读取该 target。": "It is at t=2, aligned with target ID 6 (AI). The model may use [<BOS>,我,喜欢] to predict AI, but it must not read that target first.",
  "15. 构造一个 Batch：随机选择 Rows，Rows 内保持连续": "15. Build a Batch: Randomize Row Starts, Keep Each Row Consecutive",
  "Optimizer step 需要多条 training windows；若把单个 tokens 随机拼成 row，原本的 chronological context 与 right-shift targets 都会断裂。": "An optimizer step can use several training windows. Randomly assembling individual tokens into a row would break both its original context and its next-token targets.",
  "把 B 个合法 start 对应的 contiguous x/y windows stack 为 [B,T]。": "Stack the contiguous x/y windows from B valid starts into tensors of shape [B,T].",
  "精确说明 torch.randint 的 exclusive upper bound 与 L≤T error。": "Specify torch.randint's exclusive upper bound and reject L≤T.",
  "Python s[a:b] 包含位置 a，不包含 b，所以恰有 b−a 项。T=4 的一道训练序列需要连续五个源 ID：前四个给 input，后四个给 target，两段重叠三项。随机数的 high 也不包含上界；把最后合法起点先写出来，再决定 randint 的 high。": "Python s[a:b] includes position a but excludes b, giving b−a items. A T=4 training sequence needs five consecutive source IDs: the first four are inputs and the last four are targets, overlapping by three. randint also excludes its upper bound. Write down the last valid start before choosing high.",
  "继续只用“我喜欢AI，AI也喜欢猫。”的 w09-readable-v1 stream。固定 B=2、T=4，并选择 legal starts [0,5]；随机性只选择 row 从哪里开始，每个 row 内仍按原 stream 顺序前进。": "Continue with the w09-readable-v1 stream for the same Chinese sentence. Fix B=2, T=4 and valid starts [0,5]. Randomness chooses where each row begins; within a row, IDs retain their original order.",
  "starts = [0,5]\n\ninputs = [\n  [0,4,5,6],  # <BOS> 我 喜欢 AI\n  [6,8,5,9],  # AI 也 喜欢 猫\n]             # [B,T] = [2,4]\n\ntargets = [\n  [4,5,6,7],  # 我 喜欢 AI ，\n  [8,5,9,10], # 也 喜欢 猫 。\n]             # [B,T] = [2,4]": "starts = [0,5]\n\ninputs = [\n  [0,4,5,6],  # <BOS> 我 喜欢 AI\n  [6,8,5,9],  # AI 也 喜欢 猫\n]             # [B,T] = [2,4]\n\ntargets = [\n  [4,5,6,7],  # 我 喜欢 AI ，\n  [8,5,9,10], # 也 喜欢 猫 。\n]             # [B,T] = [2,4]",
  "每行需要 T+1 个 source IDs，才能得到 T 个 inputs 与 T 个 shifted targets": "Each row needs T+1 source IDs to produce T inputs and T shifted targets.",
  "<BOS> 我 喜欢 AI": "<BOS> 我 喜欢 AI",
  "我 喜欢 AI ，": "我 喜欢 AI ，",
  "AI 也 喜欢 猫": "AI 也 喜欢 猫",
  "也 喜欢 猫 。": "也 喜欢 猫 。",
  "每个 batch row 使用同一个 start 的 input/target slices；输出 interface 是两张 [2,4] integer tensors。": "Each batch row uses input and target slices derived from the same start. The output is two integer tensors of shape [2,4].",
  "torch.randint 的 high=7 不包含 7，恰好产生七个合法 starts；需要更多 rows 时可有放回采样。": "torch.randint with high=7 excludes 7 and can produce exactly the seven valid starts. Sampling more rows may select the same start again.",
  "high=len(source)−T+1 会允许 invalid start 7。": "high=len(source)−T+1 would permit invalid start 7.",
  "inputs 与 targets 必须来自同一 start，不能分别随机抽取。": "Inputs and targets must use the same sampled starts, not independently sampled ones.",
  "B 是 batch rows 数，不是 Vocabulary candidates。": "B counts batch rows, not vocabulary candidates.",
  "这个 [2,4] batch 只属于 Week 9 artifact，不能直接送入 Week 10 block_size=2 模型。": "This [2,4] batch belongs to the Week 9 artifact. It cannot be passed directly to the Week 10 model with block_size=2.",
  "为什么每个 row 要从 source 读取 T+1 个 IDs？": "Why does each row require T+1 source IDs?",
  "前 T 个组成 input，向右一位的后 T 个组成 target；最后一个 input 的 next-token label 需要额外的第 T+1 个 source ID。": "The first T form the input; the T IDs one position to the right form the target. The final input's next-token label requires that extra source ID.",
  "10. Special Tokens：由谁添加边界必须只有一个答案": "10. Special Tokens: Give Boundary Insertion One Owner",
  "模型需要文档边界、padding 或 unknown control IDs，但若 encoder 与 data pipeline 都手动添加 EOS，同一文档会出现重复边界。": "The model may need document-boundary, padding or unknown-token IDs. But if both the encoder and the data pipeline add EOS, each document gets duplicate boundaries.",
  "定义 w09-readable-v1 的四个 reserved IDs 与各自职责。": "Define the four reserved IDs in w09-readable-v1 and their separate roles.",
  "用 encode_content(add_special_tokens=False) 与 encode_document(add_special_tokens=True) 明确分离内容和完整文档 API。": "Separate the content-only and document APIs explicitly: encode_content(add_special_tokens=False) versus encode_document(add_special_tokens=True).",
  "在 Week 9 的可读快照中，0..3 是 reserved specials，内容 entries 从 4 开始。它们是 model-visible Vocabulary entries，不是输入中看见字符 <EOS> 就自动产生的魔法。是否显示、mask、停止生成或忽略 loss 都需要调用方与模型 API 明确约定。": "In Week 9's readable snapshot, IDs 0..3 are reserved specials and content entries begin at 4. They occupy vocabulary entries visible to the model; typing the literal characters <EOS> does not automatically invoke their control behavior. Display, masking, stopping generation and loss exclusion all require explicit API agreements.",
  "文档起点 / 初始 context": "Document start / initial context",
  "encode_document 在最前添加一次": "encode_document adds it once at the beginning",
  "文档终点": "Document end",
  "encode_document 在最后添加一次；generation 可把它当 stop candidate": "encode_document adds it once at the end; generation may use it as a stop candidate",
  "把不等长 rows 补成 rectangle": "Fill unequal-length rows to form a rectangle",
  "通常在 attention / loss 中 mask": "Usually excluded through attention and loss policies",
  "word-only fallback 的 unknown marker": "Unknown-token marker for this limited content vocabulary",
  "有损；不是 byte fallback 的必需品": "Lossy; not inherently required by byte fallback",
  "API result for 我喜欢AI，AI也喜欢猫。": "API result for the running Chinese sentence",
  "[<BOS>,我,喜欢,AI,，,AI,也,喜欢,猫,。,<EOS>]": "[<BOS>,我,喜欢,AI,，,AI,也,喜欢,猫,。,<EOS>]",
  "from w09_readable_v1 import (\n    BOS_ID,\n    EOS_ID,\n    W09_READABLE_V1,\n    encode_content,\n    encode_document,\n)\n\ntext = \"我喜欢AI，AI也喜欢猫。\"\ncontent_ids = encode_content(text)\ndocument_ids = encode_document(text)\n\nassert document_ids == [BOS_ID, *content_ids, EOS_ID]\nassert W09_READABLE_V1.decode(\n    document_ids,\n    skip_special_tokens=True,\n) == text\n\n# Wrong: encode_document already owns both boundaries.\n# duplicated = [BOS_ID, *encode_document(text), EOS_ID]": "from w09_readable_v1 import (\n    BOS_ID,\n    EOS_ID,\n    W09_READABLE_V1,\n    encode_content,\n    encode_document,\n)\n\ntext = \"我喜欢AI，AI也喜欢猫。\"\ncontent_ids = encode_content(text)\ndocument_ids = encode_document(text)\n\nassert document_ids == [BOS_ID, *content_ids, EOS_ID]\nassert W09_READABLE_V1.decode(\n    document_ids,\n    skip_special_tokens=True,\n) == text\n\n# Wrong: encode_document already owns both boundaries.\n# duplicated = [BOS_ID, *encode_document(text), EOS_ID]",
  "Special IDs 与内容 IDs 都占用同一 Vocabulary axis，但必须有独立的控制角色与 insertion policy。": "Special IDs and content IDs share the vocabulary axis, but specials need explicit control roles and insertion policies.",
  "raw text 中出现字面字符 <EOS> 不表示 tokenizer 一定把它识别为 reserved EOS。": "The literal text <EOS> in raw input is not necessarily recognized as the reserved EOS token.",
  "encode_document 已经加边界时，不要再手动 append EOS 或 prepend BOS。": "If encode_document has already added boundaries, do not append EOS or prepend BOS again.",
  "EOS 高概率不保证 generation 一定在理想位置停止；stop rule 仍需显式实现。": "A high EOS probability does not guarantee stopping at the desired point. Generation still needs an explicit stopping rule.",
  "这些 special IDs 属于 Week 9 artifact；Week 10 的 mini-gpt-v1 明确没有 specials。": "These special IDs belong to the Week 9 artifact. Week 10's mini-gpt-v1 explicitly has no special tokens.",
  "为什么 encode_document(text)+[EOS] 会破坏本节约定？": "Why does encode_document(text)+[EOS] violate this section's convention?",
  "encode_document(add_special_tokens=True) 已在末尾放置一次 ID 1；再次 append 会制造两个 EOS。需要无边界内容时应调用 encode_content。": "encode_document(add_special_tokens=True) already places ID 1 at the end. Appending it again creates two EOS tokens. Use encode_content when you want content without boundaries.",
  "11. Padding 与 Attention Mask：矩形 Shape 不等于有效数据": "11. Padding and Attention Masks: Rectangular Does Not Mean Valid",
  "真实文档长度不同，而 batch 必须是矩形 [B,T]。若仅补 PAD 却不告诉 Attention 与 loss 哪些 slots 无效，模型会把 shape filler 当成文字。": "Documents have different lengths, but a batch must have rectangular shape [B,T]. If we add PAD without telling attention and loss which slots are invalid, the model may treat shape fillers as text.",
  "分开 PAD token ID、attention visibility 与 target loss inclusion。": "Separate the PAD input ID, attention visibility and inclusion of targets in loss.",
  "说明 causal mask 处理未来位置，padding mask 处理无效 slots，两者不可互换。": "A causal mask handles future positions; a padding mask handles invalid slots. Neither replaces the other.",
  "控制项": "Control",
  "先问一句话": "Question to ask first",
  "这个位置能否偷看后面的真实答案？": "Could this query peek at a real answer to its right?",
  "这个位置是不是为了凑矩形而填进去的？": "Was this slot added only to make the tensor rectangular?",
  "这一次预测有没有有效答案，是否要计分？": "Does this prediction have a valid target, and should it count toward loss?",
  "三个控制可以同时存在，不能因名字里都有 mask 就交换用法。−100 只是本例“这题不计分”的记号，不属于词表，绝不能拿去查询 embedding。": "All three controls can coexist. Sharing the word “mask” does not make them interchangeable. Here, −100 means “do not score this target”; it is not in the vocabulary and must never be used for embedding lookup.",
  "保留主句“我喜欢AI，AI也喜欢猫。”作为 row 0，并用较短的“我喜欢AI。”作为 row 1 的 padding illustration。两者均由 w09-readable-v1 的 encode_document 产生；本表临时把 batch width 记作 T_pad=11，避免与后面 training-window T=4 混淆。": "Use “我喜欢AI，AI也喜欢猫。” as row 0 and the shorter “我喜欢AI。” as row 1. Both come from w09-readable-v1's encode_document. Call this padded batch width T_pad=11 so it is not confused with the later training-window length T=4.",
  "右侧 padding 后 input_ids 与 attention_mask 均为 [B,T_pad]=[2,11]": "After right padding, input_ids and attention_mask both have shape [B,T_pad]=[2,11].",
  "0: 我喜欢AI，AI也喜欢猫。": "0: 我喜欢AI，AI也喜欢猫。",
  "1: 我喜欢AI。": "1: 我喜欢AI。",
  "占住矩形 tensor 的 unused slot": "Occupies an unused slot in the rectangular tensor",
  "禁止 query 把 PAD key 当作上下文；具体 API 还会处理 padded queries": "Prevents queries from using PAD keys as context; the API also needs a policy for padded queries",
  "禁止任何 query 读取 future key，不判断 PAD": "Prevents queries from reading future keys; does not itself identify PAD",
  "让 padded target 不计入 cross entropy": "Excludes padded targets from cross entropy",
  "一个常见 decoder-only allowed-key rule 同时要求 key 不在未来且不是 padding；实际 mask layout 应遵循 model API。": "A common decoder-only visibility rule allows a key only if it is not in the future and is not padding. Follow the specific model API's mask layout and boolean conventions.",
  "input / padding-validity mask 是 batch-by-time；causal mask 是 query-by-key positions，并可 broadcast 到 batch/head axes。": "The input padding-validity mask is batch-by-time. The causal mask is query-by-key and can broadcast across batch/head axes.",
  "接下来把规则落到一个较短的 T_mask=7 next-token batch。Row 0 从主句取连续八个 source IDs，形成七组 input→target；Row 1 的完整短文档只有六个 IDs，所以 input 补一个 PAD，同时把“EOS 后没有本文件内 next token”和 PAD 对应的 targets 都设为 -100。": "Now apply these rules to a shorter next-token batch with T_mask=7. Row 0 uses eight consecutive source IDs, yielding seven input→target pairs. Row 1's entire short document has six IDs: append one PAD to its input, and set both the target after EOS and the target for the PAD slot to -100.",
  "ID 2 是模型输入中的 PAD；-100 不是 Vocabulary ID，而是本例交给 Cross Entropy 的 ignore_index sentinel": "ID 2 is the model's PAD input. -100 is not a vocabulary ID; it is the ignore_index sentinel passed to Cross Entropy here.",
  "0: 主句 prefix": "0: Prefix of the main sentence",
  "1: 完整短文档": "1: Complete short document",
  "三个控制作用于不同对象：可见的时间方向、有效的 key，以及需要计分的 target": "The three controls apply to different things: temporal visibility, valid keys and targets that should be scored.",
  "它回答的问题": "Question it answers",
  "row 1 的具体效果": "Specific effect on row 1",
  "query t 能否读取 future key j>t？": "May query t read a future key at j>t?",
  "t=4（。）只能读取 key positions 0..4": "t=4 (。) may read only key positions 0..4",
  "query 能否把无效 PAD key 当作上下文？": "May a query use an invalid PAD key as context?",
  "所有 query 都不能读取 position 6 的 PAD key": "No query may read the PAD key at position 6",
  "这个 position 的预测要不要计分并产生 gradient？": "Should this position's prediction contribute to loss and its gradients?",
  "positions 5、6 的 targets=-100，不参与平均 loss": "Targets at positions 5 and 6 are -100 and are excluded from mean loss",
  "Loss 的分母只计算 12 个有效 next-token targets；PAD slot 与本例中 EOS 后的无标签 slot 都不计分。": "The loss denominator counts only the 12 valid next-token targets. Neither the PAD slot nor the unlabeled slot after EOS is scored in this example.",
  "为什么 padding query 仍可能产生一行输出？": "Why can a padded query still produce an output row?",
  "上面的 allowed rule 只排除 PAD keys；位于 PAD slot 的 query 仍可能读到左边真实 keys。只要它的 target 被 -100 忽略，该输出不会进入本例 loss。某些模型 API 会进一步处理 padded queries，必须以具体实现的 contract 为准。": "The visibility rule above excludes PAD keys only. A query in a PAD slot can still read real keys to its left. Its output does not enter this example's loss because its target is ignored with -100. Some model APIs additionally handle padded queries; follow the actual implementation contract.",
  "Padding conventions 会变化": "Padding conventions vary",
  "有些 API 左 pad、有些右 pad；有些组合 additive mask，有些接收 booleans。必须读取模型 contract，而不是根据 0 的外观猜它是 token ID、attention flag 还是 ignored target。": "Some APIs left-pad, others right-pad; some use additive masks, others booleans. Read the model contract instead of guessing whether a zero is a token ID, an attention flag or an ignored target.",
  "只在 loss 中忽略 PAD 仍可能允许 Attention 把 PAD 当作 context。": "Ignoring PAD only in loss may still let attention use PAD as context.",
  "attention_mask 的 0 是控制 flag，不是“token ID 0=<BOS>”。": "A zero in attention_mask is a control flag, not “token ID 0=<BOS>”.",
  "-100 是 loss sentinel，不得拿去做 embedding lookup 或 decode。": "-100 is a loss sentinel, not an ID for embedding lookup or decoding.",
  "causal mask 与 padding mask 解决不同问题。": "Causal and padding masks solve different problems.",
  "EOS 与 padding 的相对顺序必须由 document/padding policy 定义。": "The document/padding policy must define the order of EOS and padding.",
  "为什么 input_ids.shape=[2,11] 仍不足以说明这个 padded batch 正确？": "Why is input_ids.shape=[2,11] insufficient to establish that the padded batch is correct?",
  "Shape 只保证矩形。还需 attention mask 排除 padding keys，并用 ignore_index 或等价 loss mask 排除 padded targets；causal mask 另行阻止 future leakage。": "Shape guarantees only a rectangle. The attention policy must exclude padding keys; ignore_index or an equivalent loss mask must exclude invalid targets. A causal mask separately prevents future leakage.",
  "6. 未见 Token：先决定信息如何保留": "6. Unseen Tokens: Decide How to Preserve Information",
  "有限 Vocabulary 不可能把未来每个专名、拼写、emoji 序列和代码片段都作为完整 entry；direct lookup 失败时必须有明确政策。": "A finite vocabulary cannot store every future name, spelling, emoji sequence and code fragment as a whole entry. Failed direct lookup needs an explicit policy.",
  "比较 <UNK>、subword/byte fallback 与 explicit error 三种策略。": "Compare three policies: <UNK>, subword/byte fallback and an explicit error.",
  "说明 unseen 是相对于 tokenizer units 而言，并不等于模型“不认识一个语言概念”。": "“Unseen” refers to the tokenizer's units; it is not the same as the model lacking knowledge of a concept.",
  "假设 w09-readable-v1 的 word-only variant 忘了把固定句中的 猫 放进 Vocabulary：它可以输出 <UNK> 的 ID 3，但所有未知片段都会坍缩到同一地址，无法 exact decode 猫。byte-capable fallback 则能保留 E7 8C AB，随后仍可重建原文。": "Suppose a limited-vocabulary variant of w09-readable-v1 omits 猫 (“cat”). It could emit <UNK>, ID 3, but distinct unknown pieces would collapse to one address and 猫 could not be reconstructed exactly. Byte fallback can retain E7 8C AB and later reconstruct the original text.",
  "猫 不可 direct lookup 时的 output": "Output when 猫 cannot be looked up directly",
  "有损：只能还原为 unknown marker": "Lossy: reconstructs only the unknown marker",
  "短，但不同未知片段共享一个 ID": "Short, but different unknown pieces share one ID",
  "一个或多个已知 smaller-piece IDs": "One or more known smaller-piece IDs",
  "若 normalization/byte policy lossless，可保留原始文字": "Can preserve the original text if normalization and byte handling are lossless",
  "k 可能大于 1，sequence 变长": "k may exceed 1, making the sequence longer",
  "不产生 IDs": "Produces no IDs",
  "调用方必须处理失败": "The caller must handle the failure",
  "适合不允许自动 replacement 的严格输入": "Useful for strict input policies that forbid automatic replacement",
  "direct mapping 可以是 partial function；完整 encoder 必须为缺项定义返回 k 个已知 IDs、一个 UNK，或 error。": "Direct lookup can be a partial function. A complete encoder must define whether a missing entry produces k known-piece IDs, one UNK or an error.",
  "固定快照实际包含 猫，所以正常 encoding 是 ID 9。malformed byte sequence 是另一类问题：它需要 UTF-8 replacement/error policy，不能与“有效但未见的词”混为一谈。": "The actual snapshot includes 猫, so it normally encodes as ID 9. Malformed byte sequences are a separate issue requiring an explicit UTF-8 replacement/error policy; do not confuse them with valid but unseen words.",
  "<UNK> 是控制 entry，不是普通自然语言单词。": "<UNK> is a control entry, not an ordinary word.",
  "<UNK> 无法 exact reconstruct 原先是哪一个 unknown piece。": "<UNK> cannot reconstruct which unknown piece was originally present.",
  "byte fallback 保留字符串不表示模型已经学习该新实体的事实或用法。": "Preserving a string through byte fallback does not mean the model has learned facts about that entity or how to use its name.",
  "为什么 byte fallback 通常比把 猫 替换成 <UNK> 更少丢失信息？": "Why does byte fallback usually lose less information than replacing 猫 with <UNK>?",
  "E7 8C AB 可重新组成 猫；<UNK> ID 3 会把所有未知内容合并为同一地址，原字符串身份已经丢失。": "E7 8C AB can be decoded back into 猫. <UNK>, ID 3, maps distinct unknown content to one address, losing the identity of the original string.",
  "7. BPE 的核心直觉：真的手算两轮 Merge": "7. BPE Intuition: Work Through Two Actual Merges",
  "Byte base 能覆盖文字，却让“我喜欢AI，AI也喜欢猫。”占 31 个 positions；需要一种可复现的方法把训练语料中常见的相邻 symbols 命名为更长 token。": "A byte base can encode the text, but “我喜欢AI，AI也喜欢猫。” occupies 31 byte positions. We want a reproducible way to give frequently adjacent symbols a single, longer token entry.",
  "把 BPE training 展开为 count → deterministic tie break → non-overlapping merge → recount。": "Expand BPE training into count → deterministic tie-break → non-overlapping merge → recount.",
  "精确保留跨 code-point pair (9C,E6)，并验证两轮后长度 31−2−2=27。": "Include the cross-code-point pair (9C,E6), and verify that two rounds reduce the length to 31−2−2=27.",
  "进入字节表前，先做一个小合并": "Before the byte tables, try one small merge",
  "独立热身序列 [A,B,A,B,C] 有相邻对 AB、BA、AB、BC，所以 AB 出现两次。把 AB 替换为新符号 M，得到 [M,M,C]，长度从 5 变为 3。接下来应在新序列重新计数，不能沿用旧表。": "The independent warm-up sequence [A,B,A,B,C] has adjacent pairs AB, BA, AB and BC. AB occurs twice. Replacing it with a new symbol M gives [M,M,C], reducing the length from 5 to 3. Count pairs again in this new sequence rather than reusing the old table.",
  "这只是说明动作，不声称 M 是正文 tokenizer 的某个 ID。现在回到下文 31 个字节、固定排序规则和两轮真实计数；原有手算表仍是本章精确例子。": "This only demonstrates the operation; M is not an ID in the chapter's tokenizer. We now return to the precise 31-byte example, fixed tie-breaking rule and two rounds of counting.",
  "先完成可读字符的两轮，再选择是否读字节实现": "First do two readable-character rounds; the byte implementation can follow",
  "设训练资料中 low 出现 5 次、lower 出现 2 次、new 出现 2 次。先把每个词拆成字符，并只统计词内相邻对。l-o 和 o-w 都出现 7 次，并列时约定按字符对字典序选择 l-o。不是“唯一最常见”。": "Suppose training data contains low five times, lower twice and new twice. Split each word into characters and count adjacent pairs within words. l-o and o-w both occur seven times. Break the tie by lexicographic pair order, choosing l-o. There is no unique most frequent pair.",
  "轮次": "Round",
  "关键频次": "Key counts",
  "合并及结果": "Merge and result",
  "初始": "Initial",
  "(l,o)→lo：low 变 [lo,w]，lower 变 [lo,w,e,r]。": "(l,o)→lo: low becomes [lo,w]; lower becomes [lo,w,e,r].",
  "重数后": "After recounting",
  "lo-w=7；其余上述频次为 2 的对不变": "lo-w=7; the other pairs previously occurring twice still occur twice",
  "(lo,w)→low：low 变 [low]，lower 变 [low,e,r]。": "(lo,w)→low: low becomes [low]; lower becomes [low,e,r].",
  "训练 tokenizer 是从语料统计并保存这两条规则。编码新词 lower 时，只按保存的先后顺序应用规则，不再现场重新统计一套词表。合并是为相邻片段建立可还原的编号，不是求和，也不是已经学到了词义。": "Tokenizer training counts the corpus and saves these two rules. Encoding a new occurrence of lower applies the saved rules in order; it does not build a new vocabulary on the spot. A merge gives adjacent pieces a reversible symbol—it is not numeric addition or proof of learned meaning.",
  "下面是独立的字节版细节，会换成中文混合文本和十六进制编号。若字节表示还不熟，先完成本段练习和主线数据入口，再回看下一节 Unicode；不影响继续学习 GPT。": "The next example independently uses mixed Chinese/English text and hexadecimal byte values. If bytes are unfamiliar, finish the readable exercise and the main data pipeline first, then return to the Unicode section. The byte details are not a prerequisite for continuing to GPT.",
  "这是一个无 pre-token boundaries 的 toy byte-level BPE training：固定句的每对相邻 bytes 都可参与计数。只列频率至少为 2 的初始 pairs；(9C,E6) 跨越 喜 的最后 byte 与 欢 的第一 byte，并在两次 喜欢 中出现。": "This toy byte-level BPE has no pre-token boundaries: every adjacent byte pair in the sentence can be counted. The initial table lists pairs occurring at least twice. (9C,E6) crosses from the final byte of 喜 to the first byte of 欢 and occurs in both instances of 喜欢.",
  "先把一次 Merge 想成“给常见相邻片段起缩写”": "Think of a merge as giving a frequent adjacent pair an abbreviation",
  "原 stream 中两次 AI 都是相邻 bytes [41,49]。建立规则 (41,49)→256 后，每次出现都用一个新 symbol 256 替换，因此这两处各少一个 position，长度从 31 变为 29。256 只是可还原为 bytes 41 49 的新地址，不是数值相加，也不是模型已经理解 AI。": "Both occurrences of AI are adjacent bytes [41,49]. After saving (41,49)→256, each occurrence becomes one symbol, 256. Two positions are saved overall, reducing length from 31 to 29. ID 256 names the recoverable bytes 41 49; it is not their sum and does not show that the model understands AI.",
  "… 41,49 … 41,49 …  （两次出现，各占两个 positions）": "… 41,49 … 41,49 …  (two occurrences, two positions each)",
  "保存 merge rule：(41,49) → token ID 256": "Save merge rule: (41,49) → token ID 256",
  "… 256 … 256 …  （两次出现，各占一个 position）": "… 256 … 256 …  (two occurrences, one position each)",
  "完整 initial repeated-pair inventory；六组 count 都是 2": "Complete initial inventory of repeated pairs; all six counts are 2",
  "两次 AI": "Both occurrences of AI",
  "两个 喜 的前两 bytes": "The first two bytes in each 喜",
  "两个 喜 的后两 bytes": "The last two bytes in each 喜",
  "两次 喜→欢 的 code-point boundary": "The code-point boundary 喜→欢 in both occurrences",
  "两个 欢 的前两 bytes": "The first two bytes in each 欢",
  "两个 欢 的后两 bytes": "The last two bytes in each 欢",
  "第 r 轮在当前一维 symbol stream 中计算每个相邻 pair 的出现次数。": "At round r, count each adjacent pair in the current one-dimensional symbol stream.",
  "确定性的 tie rule": "Deterministic tie-breaking",
  "先最大化 count；若并列，则把每个十六进制 symbol 读成整数，并按 integer pair (a,b) 做 lexicographic ascending comparison。初始最小者是 (0x41,0x49)，不是按屏幕文字或语言边界选。": "First maximize the count. For a tie, read each hexadecimal symbol as an integer and choose the lexicographically smallest integer pair (a,b). Initially that is (0x41,0x49). The choice is not based on displayed text or linguistic boundaries.",
  "Round 1 后必须重新计数。两个 [41 49] 的邻居分别不同，因此没有产生新的 repeated adjacent pair；其余五个 repeated pairs 仍各出现两次。": "Recount after round 1. The two [41 49] occurrences have different neighbors, so no new repeated pair appears. Each of the other five repeated pairs still occurs twice.",
  "按 integer-pair lexicographic order 排列；0x96 小于 0x9C、0xAC、0xE5、0xE6": "Sorted by lexicographic integer-pair order; 0x96 is less than 0x9C, 0xAC, 0xE5 and 0xE6",
  "Round 2\nall five repeated pairs tie at count 2\nlexicographically smallest integer pair = (0x96, 0x9C)\n\n(96,9C) -> new token 257, displayed [96 9C]\n\n喜 at each occurrence is now: E5 [96 9C]\nlength: 29 - 2 = 27 symbols\n\ntotal after two rounds: 31 - 2 - 2 = 27": "Round 2\nall five repeated pairs tie at count 2\nlexicographically smallest integer pair = (0x96, 0x9C)\n\n(96,9C) -> new token 257, displayed [96 9C]\n\n喜 at each occurrence is now: E5 [96 9C]\nlength: 29 - 2 = 27 symbols\n\ntotal after two rounds: 31 - 2 - 2 = 27",
  "每一轮只根据 tokenizer-training stream 与已声明 tie rule 选 pair，再替换 non-overlapping occurrences。": "Each round chooses a pair using only the tokenizer-training stream and the declared tie-break, then replaces its non-overlapping occurrences.",
  "下面的程序不是伪代码：它从固定句的 31 个 bytes 开始，按本节声明的 count 与 tie rule 训练两轮，再把这两条 frozen merges 应用于同一句文字。最后先展开每个 learned symbol 的 bytes，拼接完整 byte stream，再统一做 UTF-8 decode。": "The following is runnable code, not pseudocode. It starts from the sentence's 31 bytes, trains two rounds using the stated counts and tie-break, then applies the two frozen merges to the same text. Decoding expands each learned symbol into bytes, joins the entire byte stream and only then decodes UTF-8.",
  "from collections import Counter\n\n\ndef count_pairs(symbols: list[int]) -> Counter:\n    return Counter(zip(symbols, symbols[1:]))\n\n\ndef choose_pair(pair_counts: Counter) -> tuple[int, int]:\n    highest_count = max(pair_counts.values())\n    tied_pairs = [\n        pair\n        for pair, count in pair_counts.items()\n        if count == highest_count\n    ]\n    return min(tied_pairs)  # lexicographic integer-pair tie break\n\n\ndef merge_non_overlapping(\n    symbols: list[int],\n    pair: tuple[int, int],\n    new_id: int,\n) -> tuple[list[int], int]:\n    merged: list[int] = []\n    replacements = 0\n    cursor = 0\n\n    while cursor < len(symbols):\n        if (\n            cursor + 1 < len(symbols)\n            and (symbols[cursor], symbols[cursor + 1]) == pair\n        ):\n            merged.append(new_id)\n            replacements += 1\n            cursor += 2\n        else:\n            merged.append(symbols[cursor])\n            cursor += 1\n\n    return merged, replacements\n\n\ntext = \"我喜欢AI，AI也喜欢猫。\"\ntraining_symbols = list(text.encode(\"utf-8\"))\nhistory: list[tuple[tuple[int, int], int, int, int]] = []\n\nassert len(training_symbols) == 31\n\nfor new_id in (256, 257):\n    pair_counts = count_pairs(training_symbols)\n    chosen_pair = choose_pair(pair_counts)\n    before_length = len(training_symbols)\n    training_symbols, replacements = merge_non_overlapping(\n        training_symbols,\n        chosen_pair,\n        new_id,\n    )\n    assert len(training_symbols) == before_length - replacements\n    history.append(\n        (chosen_pair, new_id, replacements, len(training_symbols))\n    )\n\nassert history == [\n    ((0x41, 0x49), 256, 2, 29),\n    ((0x96, 0x9C), 257, 2, 27),\n]\n\n\ndef encode_with_frozen_merges(\n    value: str,\n    merges: list[tuple[tuple[int, int], int, int, int]],\n) -> list[int]:\n    symbols = list(value.encode(\"utf-8\"))\n    for pair, new_id, _training_replacements, _training_length in merges:\n        symbols, _ = merge_non_overlapping(symbols, pair, new_id)\n    return symbols\n\n\nencoded = encode_with_frozen_merges(text, history)\nassert encoded == training_symbols\nassert len(encoded) == 27\n\n# A learned token stores a byte sequence, so decoding expands first.\ntoken_bytes = {byte_id: bytes([byte_id]) for byte_id in range(256)}\nfor pair, new_id, _count, _length in history:\n    left, right = pair\n    token_bytes[new_id] = token_bytes[left] + token_bytes[right]\n\nrestored_bytes = b\"\".join(token_bytes[token_id] for token_id in encoded)\nrestored_text = restored_bytes.decode(\"utf-8\", errors=\"strict\")\nassert restored_text == text": "from collections import Counter\n\n\ndef count_pairs(symbols: list[int]) -> Counter:\n    return Counter(zip(symbols, symbols[1:]))\n\n\ndef choose_pair(pair_counts: Counter) -> tuple[int, int]:\n    highest_count = max(pair_counts.values())\n    tied_pairs = [\n        pair\n        for pair, count in pair_counts.items()\n        if count == highest_count\n    ]\n    return min(tied_pairs)  # lexicographic integer-pair tie break\n\n\ndef merge_non_overlapping(\n    symbols: list[int],\n    pair: tuple[int, int],\n    new_id: int,\n) -> tuple[list[int], int]:\n    merged: list[int] = []\n    replacements = 0\n    cursor = 0\n\n    while cursor < len(symbols):\n        if (\n            cursor + 1 < len(symbols)\n            and (symbols[cursor], symbols[cursor + 1]) == pair\n        ):\n            merged.append(new_id)\n            replacements += 1\n            cursor += 2\n        else:\n            merged.append(symbols[cursor])\n            cursor += 1\n\n    return merged, replacements\n\n\ntext = \"我喜欢AI，AI也喜欢猫。\"\ntraining_symbols = list(text.encode(\"utf-8\"))\nhistory: list[tuple[tuple[int, int], int, int, int]] = []\n\nassert len(training_symbols) == 31\n\nfor new_id in (256, 257):\n    pair_counts = count_pairs(training_symbols)\n    chosen_pair = choose_pair(pair_counts)\n    before_length = len(training_symbols)\n    training_symbols, replacements = merge_non_overlapping(\n        training_symbols,\n        chosen_pair,\n        new_id,\n    )\n    assert len(training_symbols) == before_length - replacements\n    history.append(\n        (chosen_pair, new_id, replacements, len(training_symbols))\n    )\n\nassert history == [\n    ((0x41, 0x49), 256, 2, 29),\n    ((0x96, 0x9C), 257, 2, 27),\n]\n\n\ndef encode_with_frozen_merges(\n    value: str,\n    merges: list[tuple[tuple[int, int], int, int, int]],\n) -> list[int]:\n    symbols = list(value.encode(\"utf-8\"))\n    for pair, new_id, _training_replacements, _training_length in merges:\n        symbols, _ = merge_non_overlapping(symbols, pair, new_id)\n    return symbols\n\n\nencoded = encode_with_frozen_merges(text, history)\nassert encoded == training_symbols\nassert len(encoded) == 27\n\n# A learned token stores a byte sequence, so decoding expands first.\ntoken_bytes = {byte_id: bytes([byte_id]) for byte_id in range(256)}\nfor pair, new_id, _count, _length in history:\n    left, right = pair\n    token_bytes[new_id] = token_bytes[left] + token_bytes[right]\n\nrestored_bytes = b\"\".join(token_bytes[token_id] for token_id in encoded)\nrestored_text = restored_bytes.decode(\"utf-8\", errors=\"strict\")\nassert restored_text == text",
  "训练决定 merge list；之后的 encoding 只按固定顺序重放它": "Training determines the merge list; subsequent encoding replays it in fixed order.",
  "只有 base byte IDs 0..255": "Only base byte IDs 0..255",
  "两次 AI 各缩短一个 position": "Each occurrence of AI saves one position",
  "两次 喜 的后两个 bytes 各缩短一个 position": "The last two bytes of each 喜 save one position",
  "按 256 再 257 的顺序应用": "Apply the merge creating 256, then the merge creating 257",
  "由输入决定": "Depends on the input",
  "本句仍为 27": "Still 27 for this sentence",
  "不重新训练、不改变 rules": "No retraining or rule changes",
  "漏掉 (9C,E6) 会把 byte-stream adjacency 错当成 visible-character boundaries。": "Omitting (9C,E6) would incorrectly impose visible-character boundaries on byte-stream adjacency.",
  "Round 1 后必须 recount；不能沿用旧排名而不检查新 symbols。": "Recount after round 1; do not reuse the previous ranking without checking the new symbols.",
  "若 pair 可以重叠（例如 [A,A,A] 中的 (A,A)），pair count 与一次 non-overlapping replacement 数不一定相同。": "For overlapping pairs—for example (A,A) in [A,A,A]—the pair count can exceed the number of non-overlapping replacements.",
  "两轮后的 喜 仍是 E5 与 [96 9C] 两个 symbols，不是完整 token。": "After two rounds, 喜 is still two symbols: E5 and [96 9C], not one complete token.",
  "Inference prompt 不会触发重新计数或创建 token 258。": "An inference prompt does not trigger recounting or create token 258.",
  "新 token 的整数 256/257 是地址，不表达“更重要”或“更有语义”。": "IDs 256 and 257 are addresses, not measures of importance or meaning.",
  "为什么 Round 2 选择 (96,9C)，而且此时 喜 仍不是一个 token？": "Why does round 2 choose (96,9C), and why is 喜 still not one token?",
  "Round 1 后五个 repeated pairs 都为 2；按 integer pair lexicographic order，首元素 0x96 最小。Round 2 只合并 喜 的后两 bytes，所以它仍表示为 E5 与 [96 9C] 两个 symbols。": "After round 1, the five repeated pairs all have count 2. Lexicographic integer-pair ordering picks the one starting with the smallest value, 0x96. It merges only the last two bytes of 喜, leaving E5 and [96 9C] as separate symbols.",
  "5. Unicode 与 UTF-8：字符身份不等于序列化 Bytes": "5. Unicode and UTF-8: Character Identity Is Not Serialized Bytes",
  "“一个字符就是一个 byte”在中文和全角标点中是错误的；若混淆 code point 与 byte，就无法正确解释 byte tokenizer 或 BPE pair。": "“One character is one byte” is false for the Chinese characters and full-width punctuation here. Distinguishing code points from bytes is essential for understanding byte tokenizers and BPE pairs.",
  "把 Unicode code point 的抽象身份与 UTF-8 bytes 的具体编码分开。": "Separate a Unicode code point's abstract identity from its concrete UTF-8 byte encoding.",
  "精确验证固定句有 13 个 code points、31 个 UTF-8 bytes。": "Verify that the fixed sentence has 13 code points and 31 UTF-8 bytes.",
  "单位或记号": "Unit or notation",
  "人话说明": "Plain-language meaning",
  "例子": "Example",
  "给一个字符身份编号": "An identifier for a code point",
  "“我”的编号写作 U+6211": "我 has the code point U+6211",
  "把字符存储/传输成每项 0–255 的字节": "Stores/transmits text as bytes, each with a value from 0 to 255",
  "“我”是三个字节 E6 88 91": "我 uses the three bytes E6 88 91",
  "0x / 十六进制": "0x / hexadecimal",
  "一种写整数的方法；A–F 表示 10–15": "A way to write integers; A–F represent 10–15",
  "0x41=65，不是第 41 个 token": "0x41=65; it does not mean the 41st token",
  "分词器自己分配的词表地址": "An address assigned within a tokenizer's vocabulary",
  "具体数值只在那份词表内有意义": "The number has meaning only under that vocabulary's mapping",
  "第一遍会区分“字符数、字节数、token 数”就够了。U+6211、0xE6 等可交给 Python ord、encode、hex 验证；不要求心算进制转换。后面的集合、映射箭头也只是把输入范围和输出范围写短。": "On a first pass, distinguish character counts, byte counts and token counts. Python's ord, encode and hex can verify U+6211 or 0xE6; mental base conversion is not required. Later set notation and mapping arrows simply abbreviate input and output ranges.",
  "Unicode 给“我”分配 U+6211；UTF-8 将这个 code point 序列化为 E6 88 91。ASCII 的 A 与 I 各占一个 byte，而本句中的七个 Han code points 和两个全角标点各占三个 bytes，因此总计 21+4+6=31。": "Unicode assigns 我 the code point U+6211. UTF-8 serializes it as E6 88 91. ASCII A and I each use one byte. This sentence has seven Han code points using three bytes each, four ASCII code points using one each, and two full-width punctuation marks using three each: 21+4+6=31 bytes.",
  "“我喜欢AI，AI也喜欢猫。”的全部 13 个 code points 与精确 UTF-8 bytes": "All 13 code points of the running sentence and their exact UTF-8 bytes",
  "喜": "喜 (first character of 喜欢)",
  "欢": "欢 (second character of 喜欢)",
  "text = \"我喜欢AI，AI也喜欢猫。\"\nutf8_bytes = text.encode(\"utf-8\")\n\nassert len(text) == 13\nassert len(utf8_bytes) == 31\nassert utf8_bytes.hex(\" \").upper() == (\n    \"E6 88 91 E5 96 9C E6 AC A2 41 49 EF BC 8C \"\n    \"41 49 E4 B9 9F E5 96 9C E6 AC A2 E7 8C AB E3 80 82\"\n)": "text = \"我喜欢AI，AI也喜欢猫。\"\nutf8_bytes = text.encode(\"utf-8\")\n\nassert len(text) == 13\nassert len(utf8_bytes) == 31\nassert utf8_bytes.hex(\" \").upper() == (\n    \"E6 88 91 E5 96 9C E6 AC A2 41 49 EF BC 8C \"\n    \"41 49 E4 B9 9F E5 96 9C E6 AC A2 E7 8C AB E3 80 82\"\n)",
  "在变成 bytes 之前，还必须回答 normalization：哪些 Unicode 序列应被当作同一种输入？例如屏幕上都像 é 的文字，可以由一个 composed code point U+00E9 表示，也可以由 e（U+0065）加 combining acute accent（U+0301）表示；identity policy 会保留差异，NFC 会把后一种规范化成前一种。": "Before encoding bytes, decide on normalization: which Unicode sequences should count as the same input? Text that looks like é can be one composed code point, U+00E9, or e (U+0065) followed by a combining acute accent (U+0301). Identity normalization preserves the distinction; NFC converts the latter to the composed form.",
  "看起来接近的文字不保证拥有相同 code points 或 bytes；采用哪种 normalization 是设计选择": "Similar-looking text need not have identical code points or bytes. Normalization is a design choice.",
  "identity 与 NFC 都保留 composed form": "Identity and NFC both preserve the composed form",
  "identity 保留两点；NFC 变为 U+00E9": "Identity preserves two code points; NFC produces U+00E9",
  "NFKC 可变为 ASCII AI；NFC 不做这项兼容折叠": "NFKC can convert it to ASCII AI; NFC does not perform this compatibility folding",
  "Normalization 没有一个永远正确的答案": "There is no universally correct normalization choice",
  "NFC 常用于统一 canonical equivalents；NFKC 会进一步折叠某些兼容字符，可能对搜索有帮助，也可能抹掉任务需要的区别。Tokenizer 必须把所选 form、版本和执行顺序保存下来，并在训练与推理时完全一致。": "NFC is often used to unify canonical equivalents. NFKC additionally folds certain compatibility characters, which may help search but can erase distinctions a task needs. Save the chosen form, relevant version and operation order, and apply them consistently in training and inference.",
  "b_i\\in\\{0,\\ldots,255\\},\\qquad \\mathrm{UTF8}(\\text{我喜欢AI，AI也喜欢猫。})\\in\\{0,\\ldots,255\\}^{31}": "b_i\\in\\{0,\\ldots,255\\},\\qquad \\mathrm{UTF8}(\\text{我喜欢AI，AI也喜欢猫。})\\in\\{0,\\ldots,255\\}^{31}",
  "byte-level base alphabet 有 256 个值；固定句初始为长度 31 的 byte vector。": "The base byte alphabet has 256 values. The fixed sentence initially becomes a byte vector of length 31.",
  "不能说“中文是三个字符的 UTF-8”；这里是某些 code points 各编码成三个 bytes。": "Do not describe UTF-8 as making Chinese “three characters.” These particular code points each encode into three bytes.",
  "不要独立 decode 一个可能位于多-byte code point 中间的 fragment。": "Do not independently decode a fragment that may end inside a multi-byte code point.",
  "source-file encoding 与 model tokenizer 是两个不同层次。": "Source-file encoding and model tokenization operate at different layers.",
  "训练时使用 NFC、推理时使用 identity 或 NFKC，会让同一可见文字走到不同 byte/token sequence。": "Using NFC during training but identity or NFKC during inference can turn similar-looking text into different byte/token sequences.",
  "为什么 A 与 我 不是各占一个 UTF-8 byte？": "Why do A and 我 not both occupy one UTF-8 byte?",
  "UTF-8 用一个 byte 编码 ASCII code point U+0041；U+6211 则需要 E6 88 91 三个 bytes。可见 code point 数与 byte 数不同。": "UTF-8 encodes ASCII code point U+0041 in one byte. U+6211 requires three bytes, E6 88 91. Code-point count and byte count are different quantities.",
  "16. Tokenizer 不负责理解语言：它只定义离散接口": "16. A Tokenizer Defines a Discrete Interface; It Does Not Understand Language",
  "更紧凑的 segmentation 会提升计算效率，但很容易被误写成 tokenizer 自己拥有词义、事实或 reasoning。": "More compact segmentation can improve computational efficiency. That does not mean the tokenizer itself possesses word meanings, facts or reasoning ability.",
  "把 tokenizer-owned integer interface 与 model-owned learned float parameters 分开。": "Separate the tokenizer's integer interface from the model's learned floating-point parameters.",
  "再次用 猫 的 ID 9 说明地址、embedding 与 contextual representation 是三件事。": "Use 猫, ID 9, to distinguish an address, an embedding and a contextual representation.",
  "w09-readable-v1 把固定句中的 猫 映射为 9，只表示 ordered Vocabulary 的第 9 个地址。任何“猫常与喜欢共同出现”的 predictive pattern，都要由 token sequences、loss、backprop 与 neural parameters 学习；数字 9 和 BPE merge 本身不含这条知识。": "Mapping 猫 to 9 in w09-readable-v1 only identifies its vocabulary address. Predictive patterns such as 猫 frequently appearing with 喜欢 must be learned from token sequences through loss, backpropagation and neural-parameter updates. Neither the number 9 nor the BPE merge contains that knowledge.",
  "raw text 我喜欢AI，AI也喜欢猫。": "Raw text: 我喜欢AI，AI也喜欢猫。",
  "Embedding 不是人工撰写的词典释义，而是从 objective 学得的 parameter rows。": "Embedding rows are parameters learned through an objective, not hand-written dictionary definitions.",
  "Subword boundaries 不是 guaranteed semantic parse tree。": "Subword boundaries are not guaranteed semantic parse trees.",
  "Byte coverage 不能自动解决 reasoning、factuality、safety 或 bias。": "Byte coverage does not automatically solve reasoning, factuality, safety or bias.",
  "ID 数值的距离不等于 token meaning 的距离。": "Distance between numeric IDs is not distance between token meanings.",
  "系统可以在哪里学到 猫 与 喜欢 经常共同出现？": "Where can the system learn that 猫 and 喜欢 often occur together?",
  "在用 token sequences 训练并由 optimizer 更新的 embeddings、Attention、FFN 与 output parameters 中；不会在静态 ID 9 或 tokenizer merge list 中自行出现。": "In embeddings, attention, FFN and output parameters updated during training on token sequences—not in the static ID 9 or a tokenizer merge list by itself.",
  "17. Week 9 最应该理解的 9 件事": "17. Nine Things to Understand from Week 9",
  "Vocabulary、BPE、special IDs、stream 和 batch 若只作为独立术语记忆，容易在真正接 model 时混用 artifacts 或把 target shift 写错。": "Memorizing vocabulary, BPE, special IDs, streams and batches as isolated terms makes it easy to mix artifacts or misalign targets when connecting a model.",
  "用九条可由固定句和本章数值例子验证的陈述回收整条数据链。": "Recover the full data path using nine statements you can check against the fixed sentence and numerical examples.",
  "把 tokenizer training / encoding 与 stream / batch 两组最常见混淆再次分开。": "Separate two especially common confusions once more: tokenizer training versus encoding, and a stream versus a batch.",
  "Token 是 tokenizer-defined compute unit，不一定是 word；一个 token 通常占一个 model position。": "A token is a tokenizer-defined computational unit, not necessarily a word. Each token normally occupies one model position.",
  "Code point、UTF-8 byte 与 token 是不同单位；normalization 决定编码前是否折叠某些 Unicode 差异。": "Code points, UTF-8 bytes and tokens are different units. Normalization determines whether certain Unicode differences are folded before encoding.",
  "Subword 在 Vocabulary size 与 sequence length 之间折中；token count 属于具体 tokenizer。": "Subwords trade off vocabulary size against sequence length. A token count belongs to a particular tokenizer.",
  "Encode 把 text 映射到 IDs；decode 按 frozen tokenizer policy 返回 text。": "Encoding maps text to IDs; decoding returns text under the frozen tokenizer's policy.",
  "Tokenizer training 构建 artifacts；encoding 只应用它们，不改变它们。": "Tokenizer training constructs artifacts; encoding applies them without changing them.",
  "Tokenizer ID mapping、normalization 与 special-token policy 必须匹配 model checkpoint。": "The tokenizer's ID mapping, normalization and special-token policy must match the model checkpoint.",
  "Causal mask、padding mask 与 loss ignore mask 分别控制未来 keys、PAD keys 与需要计分的 targets。": "Causal, padding and loss-ignore masks respectively control future keys, invalid padding keys and targets that should be scored.",
  "Token stream 通过右移一位提供 inputs 与 next-token targets。": "Shifting a token stream by one position provides inputs and next-token targets.",
  "Batch 把连续、已右移的 windows stack 为 inputs,targets:[B,T]。": "A batch stacks consecutive, correctly shifted windows into inputs and targets of shape [B,T].",
  "从固定句到 logits 的每一步只改变已说明的单位或 axes。": "Each step from the fixed sentence to logits changes only the units or axes explicitly described.",
  "用 AI 自查第 5 与第 8 条：BPE training 学到 (41,49)→256 后，encoding 两次出现的 AI 只应用该 rule，不重新计数；在 i=0 的可读 stream example 中，AI 是 t=3 的 input，右移 target 是 ，。": "Use AI to check statements 5 and 8. After BPE training saves (41,49)→256, encoding both AI occurrences applies the rule without recounting. In the readable-stream example at i=0, AI is the input at t=3 and its shifted target is ，.",
  "本章固定 artifact recap": "Recap of the fixed artifact",
  "w09-readable-v1：V=11、special IDs 0..3、document stream [0,4,5,6,7,6,8,5,9,10,1]、L=11、training-window T=4、七个 legal starts 0..6。": "w09-readable-v1: V=11; special IDs 0..3; document stream [0,4,5,6,7,6,8,5,9,10,1]; L=11; training-window T=4; seven valid starts, 0..6.",
  "九条不能缩写成“token=word”或“BPE 学懂常用词”。": "Do not compress these ideas into “token=word” or “BPE understands common words.”",
  "Readable snapshot 与两轮 toy BPE 的 token inventory 是两个明确标注的教学 artifacts。": "The readable snapshot and the two-round toy BPE vocabulary are separate, explicitly labeled teaching artifacts.",
  "Shape 相同不能修复 ID mapping 不一致。": "Matching shapes cannot repair an inconsistent ID mapping.",
  "请用固定句中的 AI 解释第 5 与第 8 条。": "Use AI in the fixed sentence to explain statements 5 and 8.",
  "第 5 条：encoding 只应用已冻结的 AI merge，不更新 pair counts。第 8 条：i=0 时 AI 作为 input ID 6 对齐下一 stream ID 7（，）作为 target。": "Statement 5: encoding applies the frozen AI merge without updating pair counts. Statement 8: at i=0, input ID 6 (AI) is aligned with the next stream ID, 7 (，), as its target.",
  "18. Week 9 → Week 10：把同一批输入直接交给模型": "18. Week 9 → Week 10: Pass the Same Batch to the Model",
  "学会 tokenizer 后，不应再被迫丢掉刚准备的主线数据。我们保留 Week 6 的五词编号，直接把同样的 x/y 交给 MiniGPT。": "Learning about tokenizers should not force you to discard the main data you already prepared. Keep Week 6's five-token mapping and pass the same x/y directly to MiniGPT.",
  "区分贯穿主线的数据入口与独立的切词比较实验。": "Distinguish the main course's data interface from independent tokenization comparisons.",
  "确认下一周模型只替换“怎样计算表示”，而不偷偷修改答案和编号。": "Confirm that next week's model changes how representations are computed—not the token IDs or answers behind the scenes.",
  "主线统一使用 course_examples/course_data.py 中的 FIVE_WORD_TOKENIZER：我=0、喜欢=1、AI=2、学习=3、猫=4。按空格切分，无 BOS/EOS/PAD/UNK。原文仍是“我 喜欢 AI”“猫 喜欢 我”“我 学习 AI”；输入 [[0,1],[4,1],[0,3]]，目标 [[1,2],[1,0],[3,2]]。": "The English main path uses FIVE_WORD_TOKENIZER in course_examples/course_data.py: you=0, like=1, AI=2, study=3, we=4. It splits on whitespace and has no BOS/EOS/PAD/UNK. The documents remain “you like AI”, “we like you” and “you study AI”; inputs are [[0,1],[4,1],[0,3]] and targets are [[1,2],[1,0],[3,2]]. Use the English example package so its displayed tokens match this edition.",
  "材料": "Material",
  "用途": "Purpose",
  "是否直接给主线 MiniGPT": "Passed directly to the main MiniGPT?",
  "五词 tokenizer + T=2 窗口": "Five-token tokenizer + T=2 windows",
  "Week 6–12 的逐步计算与机制演示": "Step-by-step calculations and mechanism demonstrations in Weeks 6–12",
  "是。V=5，ID 含义一直不变。": "Yes. V=5 and the ID meanings remain unchanged.",
  "比较切词、未知字符、特殊 token 与字节合并": "Compare segmentation, unknown characters, special tokens and byte merging",
  "否。它们是独立实验，不重解释已有整数。": "No. These are independent experiments; they do not reinterpret existing integers.",
  "最终独立文档项目的字符 tokenizer": "Character tokenizer for the final independent-document project",
  "Week 12 明确开启一个新实验": "Week 12 explicitly starts a new experiment",
  "使用同一模型类，但显式新建词表、配置和 checkpoint。": "Reuses the model class, but creates a new vocabulary, configuration and checkpoint explicitly.",
  "# 可独立运行：在 course_examples 目录中执行。\nimport torch\nfrom course_data import FIVE_WORD_TOKENIZER, DEMO_DOCUMENTS, make_windows\n\nx, y = make_windows(DEMO_DOCUMENTS, FIVE_WORD_TOKENIZER, block_size=2)\ninputs = torch.tensor(x, dtype=torch.long)\ntargets = torch.tensor(y, dtype=torch.long)\nprint(inputs.tolist())   # [[0,1],[4,1],[0,3]]\nprint(targets.tolist())  # [[1,2],[1,0],[3,2]]\n# Week 10 接入：logits, loss = model(inputs, targets)": "# Standalone: run from the English course_examples directory.\nimport torch\nfrom course_data import FIVE_WORD_TOKENIZER, DEMO_DOCUMENTS, make_windows\n\nx, y = make_windows(DEMO_DOCUMENTS, FIVE_WORD_TOKENIZER, block_size=2)\ninputs = torch.tensor(x, dtype=torch.long)\ntargets = torch.tensor(y, dtype=torch.long)\nprint(inputs.tolist())   # [[0,1],[4,1],[0,3]]\nprint(targets.tolist())  # [[1,2],[1,0],[3,2]]\n# Week 10 connects the model: logits, loss = model(inputs, targets)",
  "本章的独立切词示例没有消失：它们帮助你判断何时需要新方案，但不是主线代码的隐含前置。你不必实现工业 BPE，才能看懂 GPT 训练。下一周保留这些 inputs/targets，只把 Bigram 的一行查分换成 embedding、位置、多个 block 和输出层。": "The independent tokenization examples still matter: they help you decide when a different scheme is needed. They are not hidden prerequisites for the main code. You do not need an industrial BPE implementation to understand GPT training. Next week keeps these inputs/targets and replaces Bigram's score-row lookup with embeddings, positions, blocks and an output head.",
  "相同词表大小不保证 ID 含义相同。": "Equal vocabulary sizes do not imply equal ID meanings.",
  "同一模型类可以用于新数据，但新词表必须显式绑定新配置与权重。": "The same model class can serve new data, but a new vocabulary must be explicitly bound to its configuration and weights.",
  "生成 prompt 必须由当前模型配套的 tokenizer 编码。": "Encode a generation prompt with the tokenizer paired with the current model.",
  "从 Bigram 换成 MiniGPT 后，这六个 target ID 应该改变吗？": "Should these six target IDs change when replacing Bigram with MiniGPT?",
  "不应该。它们来自同一训练文字的右移，不由模型架构决定。改变的是产生 logits 的函数。现在模型可以区分“我 喜欢”和“猫 喜欢”，但两个相同“我”的起始上下文仍不能因目标不同而产生两种确定性预测。": "No. They come from shifting the same training text, not from the model architecture. What changes is the function producing logits. The model can now distinguish “you like” from “we like”, but two identical initial contexts “you” still cannot deterministically produce different predictions merely because their targets differ.",
  "怎样把“我喜欢AI，AI也喜欢猫。”稳定地变成模型可用的整数 IDs，同时不混淆 tokenizer training、encoding 与模型学习？": "How can we reliably turn “我喜欢AI，AI也喜欢猫。” into usable integer IDs while keeping tokenizer training, encoding and model learning distinct?",
  "比较 character、word、UTF-8 byte 与 subword 四种单位对 V、L、覆盖能力和计算成本的影响。": "Compare character, word, UTF-8 byte and subword units in terms of V, L, coverage and computational cost.",
  "区分 code point、UTF-8 byte、BPE symbol、token 与 token ID，并说明 normalization 为什么属于 tokenizer 协议。": "Distinguish code points, UTF-8 bytes, BPE symbols, tokens and token IDs; explain why normalization belongs to the tokenizer protocol.",
  "手算 byte-level BPE 的两轮 pair count、tie break、merge 与长度变化。": "Work through two byte-level BPE rounds: pair counts, tie-breaking, merging and length changes.",
  "区分 tokenizer training 与 encoding，并把 tokenizer artifacts 与 model checkpoint 绑定。": "Distinguish tokenizer training from encoding and bind tokenizer artifacts to model checkpoints.",
  "沿 text → tokens → stream [L] → shifted examples [T] → padded batch [B,T] 完成一条可审计的数据路径。": "Follow an auditable path: text → tokens → stream [L] → shifted examples [T] → a batch [B,T], with padding only where needed.",
  "分别构造 causal mask、padding mask 与 loss mask，解释它们各自阻止哪一种错误。": "Construct causal, padding and loss masks separately and explain which errors each prevents.",
  "在 Week 10 前明确结束 w09-readable-v1，并切换到不兼容的 mini-gpt-v1。": "Before Week 10, stop using the independent w09-readable-v1 ID space and return to the incompatible five-token mini-gpt-v1 main path."
}
